[pytest]
testpaths = tests
python_files = test_*.py
python_classes = Test*
python_functions = test_*
addopts = -ra -q --strict-markers
# 默认 60s 超时（pytest-timeout）；并发测试可以单独 @pytest.mark.timeout(120)
timeout = 60