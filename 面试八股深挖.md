# 🎯 kb-v2 面试八股深挖（对着项目生挖）

> 这不是背诵稿，是"逼你想明白"的清单。每题先自己想 10 秒，再看"答法"，
> 然后看"挖深"——那才是面试官真正会追问的。
> 用途：把项目吃透到"任何角度都问不倒"。
>
> 更新时间：2026-09-05（新增标签自治、流式输出、长期记忆、pytest 50/50）

---

## 第一层：开场介绍（30 秒版本）

**Q1：介绍一下你的项目。**
> 答法：我做了一个**分层架构的个人知识库 RAG 系统 + AI 助手**。用户把笔记（md/docx/pdf）丢进去，
> 系统自动增量入库——解析、分块、向量化、登记台账、**自动打标签（标签自治体系）**；然后基于 LangGraph
> 跑多 Agent 问答：Planner 规划检索方案 → Retriever 从 Chroma 向量库召回 → tool_agent 判断要不要
> 调工具（算数/联网，ReAct）→ Answerer 组织回答（**SSE 流式输出**）→ Reviewer 审查打回重写。
> 外层是 FastAPI + 豆包风格左侧边栏前端，支持上传自动分类+人工审查、内容去重、Function Calling 工具、
> **短期会话记忆 + 跨会话长期记忆**、进度条、会话导出、PDF 读取。
> 当前数据：103 篇文档、3766 个向量片段、90 个自治标签，pytest 50/50 通过。

**挖深**：你"分层"分了几层？为什么这么分？
> 三层：ingestion（数据怎么进）、storage（数据怎么存）、agents（怎么用数据回答），
> 加一层基础设施（llm/embedding/prompts/memory）。依赖单向向下，storage 不知道 agents 存在——
> 这样每一层能独立测试、独立替换，比如换向量库只动 storage 层。

---

## 第二层：RAG 基本功（必问）

**Q2：什么是 RAG？你项目里怎么实现的？**
> RAG = 检索增强生成。大模型有幻觉、知识有截止日期，所以不直接问它，
> 而是先从自己的知识库里**检索**到相关资料，把资料拼进 prompt 再让它回答——
> 回答有依据、可溯源（我前端会显示参考来源和相似度）。

**挖深 1：为什么不全库都塞给模型？**
> token 有限制 + 无关信息会干扰回答。所以用**向量相似度**只取最相关的 top_k（我配 4）段。

**挖深 2：向量检索的原理？**
> 文本 → embedding 模型 → 512 维向量。语义相近的文本向量夹角小（余弦相似度高）。
> 存的时候每段存 文本+向量；查的时候问题也转向量，和库里所有向量比余弦，取最高分。

**挖深 3：为什么不直接 SQL like 搜索？**
> like 是"字面匹配"——搜"装饰器"匹配不到"给函数加功能"这种同义表达。
> 向量是**语义匹配**——意思相近就能命中。这也是 RAG 的核心价值。

**Q3：Embedding 模型怎么选的？为什么用 bge-small-zh？**
> 中文检索场景。BAAI 的 bge 系列在中文上效果好，small 版 512 维够用且本地跑得快、
> 免费离线（sentence-transformers 加载，设了 HF_HUB_OFFLINE）。检索时按 bge 官方建议
> 给 query 加指令前缀，效果提升明显。

**挖深：Embedding 和 LLM 是同一个模型吗？**
> 不是。Embedding 是专门训练来做"语义向量"的小模型（bge）；LLM 是生成回答的大模型
> （DeepSeek）。各司其职：一个管"找到相关的"，一个管"把相关的讲清楚"。

**Q4：分块参数 chunk_size=400 / overlap=120 怎么定的？**
> 400 字是**对着模型规格定的**：bge-small-zh-v1.5 的编码上限是 512 token，中文约 1 字≈1 token，
> 400 字给模型留足余量。（最初用 750 字，实测发现超长部分 embedding 被截断，检索质量受影响——
> 这是我踩过的坑，任何模型参数都要对着规格核对，不能照搬旧项目经验值。）
> overlap=120 是为了**防止语义被拦腰切断**——比如一段话横跨两个块，重叠能保住完整性。

**挖深：分块太大会怎样？太小呢？**
> 太大：块里混多个主题 → 检索不精准，还费 token，甚至超 embedding 上限被截断。
> 太小：单块信息不完整 → 召回但答不全。
> 400/120 是"检索精度"和"上下文完整"的折中，这就是要可配置（放 kb_config.yaml）的原因。

---

## 第三层：架构与存储决策（深挖点）

**Q5：为什么自建 doc_index.json 台账，不直接用向量库的元数据？**
> 三个原因：①**增量更新需要**——向量库查"文件改没改"不方便，我按 hash 对比台账，
> 没变的文件跳过不重复向量化；②**删除检测**——台账有而 raw 没有 → 文件被删 →
> 自动清它的向量片段；③**可调试**——JSON 随时能打开看，向量库是黑盒。
> 另外台账的 id 仿 inode，稳定不变，将来做文档关联有锚点。

**挖深：增量更新的流程？怎么判断文件"改过"？**
> scanner 扫 raw 算每个文件的 **md5 hash**，和台账里存的 hash 比：
> 新增（台账没有）/ 修改（hash 变了）/ 未变（hash 同，跳过）/ 删除（台账有、文件没了）。
> 只对 新增+修改 做分块向量化——省大量 embedding 时间。

**挖深：为什么用 md5？会不会冲突？**
> 个人知识库场景，md5 足够（冲突概率可忽略）。如果严谨可以换 sha256——成本一样。

**Q6：向量库为什么用 Chroma？能换吗？**
> 个人项目要本地、轻量、免运维。Chroma 是嵌入式向量库（像 SQLite 之于 MySQL），
> 一个目录搞定持久化。将来数据量大了可换 Milvus/pgvector——因为 storage 层是独立封装的，
> 换实现只改 vector_store.py 一个文件，上层无感。**这就是分层的收益**。

**Q7：0.45 相似度阈值是怎么来的？**
> 经验调参 + 可配置。作用：分数 < 0.45 视为"没检索到"，不给弱资料硬答——宁可转联网搜索或
> 如实说不知道，也不拿无关片段让模型编。**RAG 的底线是诚实**。
> 阈值太低会答非所问还带假来源，太高会漏答。

**挖深：阈值哪里生效？双保险？**
> retriever 里低于阈值转联网；ask() 返回前再校验一次最高分，双保险防止把弱资料当来源
> 显示给用户（旧项目踩过"乱来源"的坑，专门修过）。

---

## 第四层：标签自治体系（项目核心亮点！必问）

**Q8：你的标签是写死的吗？怎么实现的？**
> **不是写死的。** 我做了一套**标签自治体系**：
> 1. **标签向量库**：所有标签存在 Chroma 的 `tags` 集合里，每条标签单独生成向量建索引（当前 90 个标签）。
> 2. **三分层打标**（阈值经 101 篇文档实测校准）：
>    - **≥0.60**：直接复用标签库已有标签（免 LLM，快）
>    - **0.45~0.60**：LLM 确认候选标签（给 Top5，让 LLM 选）
>    - **<0.45**：LLM 生成 1 个新标签 → 写入标签向量库 → 后续新内容就能检索到这个刚加的标签
> 3. **自治闭环**：新标签自动写回标签库，全程不用改代码。

**挖深 1：阈值 0.60/0.45 是怎么定的？为什么不是 0.85？**
> 这是我**用真实数据校准**的结果。最初直觉建议 0.85（"高相似度才复用"），但实测 101 篇文档的
> Top1 标签相似度分布：min=0.467 / p50=0.597 / max=0.741——**0.85 阈值命中率 0%，方案完全失效**。
> 原因：bge 在"短标签 vs 长文档"场景下分数天然偏低（标签几个字，文档几百字，语义空间距离大），
> 0.85 是"句子对句子"的阈值，不适合"标签对文档"。
> 实测后改为三分层：0.60 复用（命中率 46.5%）、0.45 LLM 确认、低于生成新标签。
> **教训：阈值不能拍脑袋，必须用真实数据分布校准。**

**挖深 2：标签自治和传统标签系统的区别？**
> 传统标签：候选表写死在代码里，新增领域要改代码、重新部署。
> 我的标签自治：标签是"活的"——系统从内容中自动发现新概念、生成新标签、写入向量库，
> 后续内容自动匹配复用。本质是**用向量检索替代硬编码候选表**，标签库随数据增长自进化。

**挖深 3：标签自治有什么风险？怎么应对？**
> 风险 1：**跨领域误匹配**——0.60 复用线在"通用编程概念"上容易撞车（如 ES6 文档匹配上"Python基础"）。
> 应对：观察误匹配率，必要时把复用线上调到 0.65（更多走 LLM 确认路径）。
> 风险 2：**标签爆炸**——LLM 生成太多细碎标签。应对：阈值分层控制，低于 0.45 才生成新标签，
> 且每次只生成 1 个；后续可加标签合并功能。
> 风险 3：**标签质量参差**——LLM 生成的标签可能不规范。应对：`_clean_tag()` 清洗（去标点、统一大小写），
> 种子标签（38 个预设高质量标签）打底。

**挖深 4：tag_store 和 tagger 的循环依赖怎么解决的？**
> tag_store 需要调 tagger 的 `_clean_tag()` 清洗标签，tagger 需要调 tag_store 的 `retrieve/add_tags`——
> 模块级互相 import 会死循环。解法：**tag_store 不在顶层 import tagger，只在函数内部延迟 import**
> （`from .tagger import _clean_tag as _ct`），循环被打破。这是 Python 循环依赖的标准解法。

---

## 第五层：上传与审查流程（项目独有亮点）

**Q9：上传流程为什么设计成"待审查"？**
> 因为 LLM 自动分类可能错。设计成：上传 → pending 区（**不正式入库**）→ LLM 建议主题 + 建议标签 →
> 人工在网页确认/改主题 → 才移入 raw/ 并入库。**分类错不会污染知识库**。
> 信任它时可以一键"自动入库"跳过审查——两种模式都给。

**挖深：pending 为什么放 raw/ 外面？**
> ingest 是递归扫 raw/ 的。如果待审查文件也在 raw/ 里会被误入库。
> pending 放 knowledge/pending/，天然隔离——这就是"目录分层防呆"的设计。

**Q10：内容级去重怎么做的？**
> 上传时对文件内容算 **md5**，分别查台账（已入库）和 pending（待审查）的 hash：
> 命中就返回 duplicate 拒绝，并告诉用户"库中已有相同内容：xxx"。同名不同内容、
> 同内容不同名都能识别——按**内容**去重而非文件名。

**挖深：为什么按内容不按文件名？**
> 同一篇笔记可能存成"装饰器.md"和"装饰器副本.md"，文件名不同内容相同——
> 按文件名去重就漏了，会重复入库两份。内容 hash 才是一篇笔记的"真身份"。

**Q11：上传支持哪些格式？怎么解析的？**
> md（直接读文本）、docx（python-docx 抽段落）、pdf（pypdf 抽文本页）。
> 分类器只读文件**开头前几页/1500 字**——省 token 又快，判断主题不需要全文。

---

## 第六层：LLM 与多 Agent（核心竞争力）

**Q12：你的 LLM 封装做了什么？为什么值得封装？**
> 所有主流厂商（DeepSeek/OpenAI/硅基流动/Ollama）都兼容 OpenAI 接口，
> 所以一个 OpenAI 客户端类 + providers 配置表就统一了。`provider: auto` 自动检测
> .env 配了哪家 key 用哪家；没 key 走 mock 假回答——**没 key 也能把流程跑通再填**。
> 上层代码永远只调 `LLMClient().chat(...)`，换模型不改业务代码。
> 另外封装了 **`chat_stream()` 流式生成**——逐 token yield 增量，供 SSE 流式输出使用。

**挖深：为什么"厂商都兼容 OpenAI 接口"这件事对你很重要？**
> 说明 LLM 调用层已经事实标准化（像 HTTP 协议），我的封装只需做配置切换，不用写多个 SDK。
> 面试官想听你知道这个行业事实 + 你利用了它。

**Q13：LangGraph 解决什么问题？为什么用它不自己写？**
> 多 Agent = 多个带状态的步骤 + 条件跳转。自己写要管状态传递、循环控制、重试，
> 容易错。LangGraph 给 StateGraph：声明节点、边、条件边，它负责执行和状态流转。
> 我重点用的是**条件边打回循环**：Reviewer 不过 → 回 Answerer 重写，最多 3 轮防死循环。

**挖深：你四个 Agent 各自 prompt 怎么设计的不让它们"串戏"？**
> 每个节点用独立 system prompt（集中在 prompts.py）：
> Planner 只输出 JSON（话题域/是否总览/拆查询）；Answerer 给"依据资料不编造"人设；
> Reviewer 给三条检查标准并规定"通过只输出 pass"。角色隔离靠 prompt 边界 + 状态字段分离。

**挖深：为什么 Planner 要"拆查询"？**
> 复杂问题一个检索词可能召回不全。比如"list 和 tuple 区别"拆成
> ["Python list tuple 区别", "可变 不可变"]——多路召回提高命中率，再合并去重。

**Q14：你的多 Agent 和"工具调用"什么关系？**
> 已经接进 graph 了——`tool_agent` 是 graph 里的一个节点（在 Retriever 之后、Answerer 之前），
> LLM 自主决定要不要调工具（ReAct：先想→动手→根据结果再想）。
> tools.py 是**工具库**（注册表 + 执行函数 + Function Calling 循环），tool_agent 节点复用它。
> 三个工具：`search_knowledge`（查库）/ `execute_python`（算数，子进程真超时）/ `web_search_tool`（联网）。
> graph 里 tool_agent 只带后两个（检索已由 Retriever 做过）。

**挖深：Function Calling 的原理？**
> 把工具描述（JSON Schema）随请求发给模型。模型不执行，只返回
> {"tool_calls": [{name, arguments}]}。程序执行真函数，把结果以 role=tool 喂回，
> 模型再基于结果答。**模型负责"决定"，代码负责"执行"**——安全边界在这。

**挖深：execute_python 怎么保证安全？**
> 用**子进程**跑（subprocess.run + timeout），超时直接杀进程——比线程安全（线程超时杀不死，
> 失控代码还在后台跑甚至拖死解释器）。子进程内存/锁全隔离。
> 还要处理：stderr（语法错误）、returncode（崩溃/exit）、代码异常转文字。
> 局限诚实说：不是真沙箱（还能读写文件/网络），对外开放必须 Docker。

**挖深：tool_agent 在"无资料"时怎么处理？（深挖陷阱）**
> 最初实现有个 bug：Retriever 检索落空（hits 空）时 Answerer 直接走"无资料"分支提前 return，
> 把工具结果 tool_result 丢了——结果"2的10次方"变成"凭常识 1024"还用不上工具。
> 修复：判断改成 `has_material = hits or tool_result`——工具结果恰恰在检索落空时最有用。
> （这个故事面试讲出来 = 展示你会从 bug 里学到架构认知）

---

## 第七层：流式输出（项目亮点，深挖点）

**Q15：你的流式输出怎么实现的？SSE 还是 WebSocket？**
> 用 **SSE（Server-Sent Events）**。实现链路：
> 1. `llm.chat_stream()`：真实流式调 LLM，逐 token yield 增量。
> 2. `graph.ask_stream()` 生成器：后台线程跑 `graph.stream(state)`，逐节点 emit
>    `{"type":"status"}`（规划/检索/工具/生成/审查，中文阶段名）；answerer 节点的增量经
>    **`threading.local` 回调**传进 queue，主线程循环消费 queue 实时 yield `{"type":"delta"}`。
> 3. `main.py /api/chat/stream`：SSE 格式 `data: {json}\n\n`，先发 start，逐事件转发，
>    结束后落库会话历史 + memory 提炼（对齐 /api/chat 的副作用）。
> 4. 前端：`fetch + res.body.getReader() + TextDecoder` 逐 chunk 解析 SSE，
>    status 显示阶段 chips，delta 追加文本（打字机光标 blink），done 后渲染 Markdown + 来源。

**挖深 1：为什么用 threading.local？直接全局变量不行吗？**
> 不行。多请求并发时，全局变量会串台——A 请求的回调被 B 请求的 answerer 调用。
> `threading.local` 是线程局部存储，每个线程有独立的回调引用，保证并发安全。
> 这是"同步图引擎 + 流式 LLM 桥接"的关键设计。

**挖深 2：节点级状态和 token 级增量是两个粒度，怎么分别处理？**
> 节点级（规划/检索/工具/生成/审查）：`graph.stream(state)` 逐节点 yield，直接 emit status 事件。
> token 级（answerer 内部 LLM 生成的每个字）：answerer 节点内部调 `_chat_stream()`，
> 每拿到一个 token 同时拼进返回文本 + 调 threading.local 回调推 queue。
> 两个粒度互不干扰，前端分别渲染（阶段 chips + 打字机文本）。

**挖深 3：为什么不用 WebSocket？**
> SSE 是单向流（服务器→客户端），正好匹配"问答输出"场景（客户端只发一次请求，服务器持续推）。
> WebSocket 是双向的，更复杂、需要管理连接生命周期。SSE 基于 HTTP，实现简单、自动重连、
> 浏览器原生支持 EventSource。**选最简单够用的方案**。

**挖深 4：流式输出结束后怎么落库？**
> SSE 流结束后（done 事件），后端在同一个请求处理函数里同步落库：
> 会话历史写入 session.json、memory 异步提炼新事实。这和非流式 `/api/chat` 的副作用对齐——
> **流式只是输出方式变了，业务逻辑不能少**。

---

## 第八层：记忆系统（短期 + 长期）

**Q16：你的记忆系统怎么设计的？短期和长期区别？**
> **双层记忆**：
> - **短期记忆（session.py）**：浏览器存 session_id → 后端按 session 存最近 6 轮问答 →
>   提问时把历史拼进 prompt。本质是把历史当上下文喂给模型，不是模型真"记住"。
>   进程内内存，重启服务就没了。
> - **长期记忆（memory.py，已接线）**：跨会话的用户画像提炼。每次对话后，LLM 从对话中提取
>   新事实（如"用户在学 Python"、"用户关注 AI Agent"），去重后存入
>   `knowledge/memory/profile.json`。提问前把记忆文本拼进 system prompt，让回答更个性化。
>   后台线程异步提炼，不拖慢响应。17 个测试用例覆盖。

**挖深 1：为什么只留 6 轮短期记忆？**
> prompt 有长度上限；历史越长越贵越慢，且太旧的信息对当前问题帮助递减。
> 6 轮是成本与效果的折中，做成常量可调。

**挖深 2：记忆和历史在 prompt 里顺序重要吗？**
> 重要。我的顺序：长期记忆（用户画像）→ 之前对话（短期历史）→ 参考资料 → 当前问题。
> 模型对**靠近结尾**的内容更敏感，所以当前问题放最后；长期记忆放最前作背景设定。

**挖深 3：长期记忆怎么防止"记忆污染"？**
> 三个机制：①**去重**——新事实和已有记忆比对，重复的不存；②**截断**——记忆有长度上限，
> 太旧的优先级低的被淘汰；③**提炼而非原文**——LLM 提炼的是"事实"而非对话原文，
> 减少噪音。局限：LLM 提炼可能有误，后续可加人工审核机制。

---

## 第九层：工程化拷问（送分也送命）

**Q17：Web 层为什么用 FastAPI？**
> 自动生成 OpenAPI 文档（/docs）、类型校验（Pydantic）、原生 async、
> 轻量。面试加分点：我提了 File Upload 用 `list[UploadFile]` 支持批量。

**Q18：入库是同步的，浏览器会卡吗？怎么解决的？**
> 会。最慢的是批量向量化。方案：**后台线程**跑 ingest，进度写全局 dict，
> 前端轮询 /api/ingest/progress 拿 {stage, percent} 显示进度条。
> 顺带说明：embed 是分批（每 16 段）跑的，否则一次调用中间没法报进度。

**Q19：测试怎么做的？**
> **pytest 50/50 通过**（约 20 秒），覆盖：
> - test_memory.py：长期记忆 17 用例（去重/截断/回灌/异常）
> - 标签自治相关：tag_store 检索/添加/种子灌入、tagger 三分层路径
> - 检索基准：benchmark_retrieval.py + 23 条 retrieval_cases.json
> - 核心逻辑：增量入库、去重、阈值、会话隔离
> 之前是端到端冒烟，后来补了 pytest——**从"手动验证"到"自动化回归"是工程化的关键一步**。

**挖深：如果让你补测试，最先测什么？**
> ① 增量入库：改文件 hash 变化才重导；② 去重：同内容不同名返回 duplicate；
> ③ 阈值：弱相关不给来源；④ 会话：同 session 记得、换 session 不串。这四个是核心逻辑。

---

## 第十层：陷阱与诚实清单（别在这翻车）

**可能被追问的薄弱点，提前准备好话术：**

1. **"你 0.45 阈值是拍脑袋吗？"**
   → 经验值 + 可配置。但标签阈值（0.60/0.45）是**用 101 篇真实数据校准**的，不是拍脑袋。
   检索阈值 0.45 还没用评测集调优，个人项目阶段可接受，可改进。

2. **"向量库 3766 段数据量太小，验证不了性能？"**
   → 承认量小，但架构为扩展设计：增量跳过、分块缓存、存储层可换、标签自治随数据增长。
   103 篇文档覆盖 AI-Agent/Python基础/学习路线等多主题，足以验证功能正确性。

3. **"Agent 是套壳吧？"**
   → 是套 LangGraph 壳，但**编排逻辑、阈值双保险、审查循环、prompt 设计、流式桥接（threading.local）、
   标签自治体系是我写的**，且我讲得清每一步为什么——套壳不丢人，讲不清才丢人。

4. **"exec 执行代码不怕被攻击？"**
   → 承认非沙箱，说明这是本地个人工具 + 有意识的安全边界认知（真开放要 Docker）。
   子进程隔离 + 超时已经比线程安全很多。

5. **"为什么不用现成 LangChain/LlamaIndex 全家桶？"**
   → 用过会讲原理。我刻意**手写 ingest/台账/审查/标签自治**来理解 RAG 本质，
   框架是编排工具不是黑盒。这个回答是加分项——展示"知其所以然"。

6. **"标签自治的标签质量怎么保证？LLM 乱生成怎么办？"**
   → 三层防护：种子标签打底（38 个预设高质量标签）、阈值分层控制（低于 0.45 才生成新标签）、
   `_clean_tag()` 清洗。已知风险是跨领域误匹配，观察后可调阈值。诚实说清楚现状 + 改进方向。

7. **"流式输出的首 token 延迟 9 秒，是不是太慢？"**
   → 承认。原因：多 Agent 流程（规划→检索→工具→生成）在 answerer 之前有 3 个节点各调一次 LLM，
   首 token 延迟是"前置节点时间"。改进方向：简单问题跳过规划/工具节点、检索结果缓存、
   模型加速。这是多 Agent 架构的固有 trade-off——质量 vs 延迟。

---

## 附：自问自测清单（背下来前先能答）

- [ ] 一条笔记从上传到能回答，经过哪几步？每步哪个文件？
- [ ] hash 在几个地方用？分别解决什么问题？
- [ ] 0.45 检索阈值在哪两个环节生效？标签三分层阈值是多少？怎么校准的？
- [ ] pending 为什么放 raw 外面？
- [ ] 多 Agent 打回循环的终止条件？
- [ ] chat / chat_with_tools / chat_stream 返回差别？
- [ ] 换向量库/换模型各要动哪层？
- [ ] 短期记忆和长期记忆差在哪？长期记忆怎么防止污染？
- [ ] 流式输出的 threading.local 回调是怎么桥接同步图引擎和流式 LLM 的？
- [ ] 标签自治的闭环是什么？新标签怎么自动写回？
- [ ] tag_store 和 tagger 的循环依赖怎么解决的？
- [ ] pytest 覆盖了哪些核心逻辑？多少用例？
