# 🕳️ kb-v2 开发记录（踩坑）

> 本文件记录 kb-v2 从零开发至今踩过的每一个坑：**现象 → 根因 → 修复 → 教训**。
> 时间范围：2026-08-31（首个 commit `4add43a`）→ 2026-09-04，共 26 个 commit。
> 状态约定：✅ 已修复 ｜ ⚠️ 已知限制（未修）｜ 🟡 设计约定（规避型）
>
> 素材来源：git 提交历史、代码内修复注释、检查记录、项目计划书风险表。全部如实，不补写没发生的事。

---

## 〇、快速索引

| 分类 | 坑数 | 关键坑 |
|---|---|---|
| 存储与数据层 | 4 | 分块超 embedding 上限 / 同名文件互删 / 台账进 git |
| Agent 与 LLM 层 | 8 | 误导入 / 合并未排序 / 工具结果被丢 / 审查依据缺失 |
| 会话与记忆 | 3 | 双 s_ 前缀 / 无兼容迁移 / 记忆孤岛 |
| Web 与前端 | 3 | GBK emoji 崩溃 / 前端脚本语法错 / chroma 遥测 |
| 依赖与工程 | 3 | httpx 版本锁 / 路径穿越 / pending 误扫 |

---

## 一、存储与数据层

### 1. 分块大小超过 Embedding 模型上限（✅）

- **现象**：入库后向量化结果被截断，检索质量受影响。
- **根因**：`chunk_size` 沿用旧项目经验值 750 字，但 bge-small-zh-v1.5 的编码上限是 **512 token**。配置注释后来写明"中文约 1 字 ≈ 1 token"，750 字明显超限，超长部分 embedding 直接被截。
- **修复**：`kb_config.yaml` 中 `chunk_size: 750 → 400`（400 字 ≈ 400 token，给模型留足余量）。
- **教训**：Embedding 模型的 token 上限和 LLM 的 token 上限是两回事；任何模型相关参数都要对着模型规格核对，不能照搬旧项目的经验值。

### 2. 不同目录同名文件互相覆盖 / 误删（✅）

- **现象**：`topicA/同名.md` 和 `topicB/同名.md` 同时存在时，删除/重导会互相删片段。
- **根因**：向量库 metadata 的 `source` 字段只存文件名（如 `同名.md`），**全局不唯一**——按 source 删除时两个文件会被当成一个。
- **修复**（提交 `3ded77f`）：
  - `source` 统一改为**相对路径**（`topic/.../文件名`），全局唯一；
  - `vector_store.delete_by_source` 增加 `topic` 参数，兼容旧格式数据清理；
  - `pipeline` 重导前按 `(source, topic)` 去重，每个文件只删一次，不再对每个 chunk 重复调删除。
- **教训**：存储层的标识必须**全局唯一**。"当前只有几个文件、看着够用"的标识，会在多目录场景爆雷。

### 3. 台账 doc_index.json 被 git 跟踪（⚠️ 未处理）

- **现象**：每次 `ingest` 后 `git status` 都显示 `knowledge/index/doc_index.json` modified。
- **根因**：`.gitignore` 忽略了 `vector_store/`、`chunks/`，却漏了 `index/`。
- **建议**：把 `knowledge/index/` 加入 `.gitignore`——台账是从 raw 算出来的衍生数据，可随时重建，不该进版本库制造噪音。
- **教训**：运行时自动生成的**数据文件**一律 gitignore，代码和衍生数据分开。

### 4. 单段落超长不切分（⚠️ 已知限制）

- **现象**：`splitter.chunk_md` 中，若**单个段落**本身超过 `chunk_size`（400），且当前缓冲区为空时，整段直接放入不切分。
- **影响**：罕见（中文笔记里超 400 字的单段落不常见），但会产生超长片段，影响检索精度。
- **修法（未来）**：段落内部再按句/字符二次切分，或用滑动窗口兜底。
- **教训**：分块器对"超长不可分割单元"要有兜底策略，不能假设输入永远规整。

---

## 二、Agent 与 LLM 层

### 5. IDE 自动补全事故：误导入 typer.cli.state（✅）

- **现象**：`kb/agents/graph.py` 第一行 `from typer.cli import state`，全文件无人使用。
- **根因**：IDE 自动补全手滑引入的死代码；靠项目里间接依赖（typer）恰好能跑，没立刻暴露。
- **修复**（提交 `3ded77f`）：删除该导入。
- **教训**：**未使用的 import 也是评审点**。"碰巧能跑"不意味着代码是对的；提交前扫一眼 diff 里的多余行。

### 6. 多查询合并后未排序，阈值误判（✅）

- **现象**：Planner 拆多个查询 → Retriever 合并去重后，用 `hits[0]["score"] < threshold` 判断是否转联网，但合并后的列表**不是按分数排序**的，`hits[0]` 可能是低分项 → 检索明明命中也误判为"弱相关"转联网。
- **修复**（提交 `3ded77f`）：
  - 合并后显式 `hits.sort(key=score, reverse=True)`；
  - `ask()` 返回前用 `max(h["score"])` 再做一次阈值校验（**双保险**，旧项目踩过"乱来源"的坑）。
- **教训**：**跨多个来源合并的结果必须显式排序**；阈值判断要基于全局最优，不是位置上的第一个。

### 7. 检索落空时把工具结果丢了（✅）

- **现象**："用 Python 算 2 的 10 次方"这类问题，Retriever 在知识库检索落空 → Answerer 走"无资料"分支提前 return，**工具已经调出来的结果 tool_result 被丢掉**，回答退化成"凭常识 1024"。
- **根因**：`answerer` 判断"有没有料"只看了 `hits`，没算 `tool_result`。而工具结果恰恰在**检索落空时最有用**。
- **修复**：`has_material = state.get("hits") or state.get("tool_result")`。
- **教训**：判断"有没有可用的料"要看**所有信息来源**，不能只盯检索这一路；流程节点间的数据传递要检查"谁可能在前一步拿到东西"。

### 8. Reviewer 审查依据不含工具结果（✅）

- **现象**：工具型答案（计算/联网）被 Reviewer 拿"空参考"审查 → 误判没依据 → 打回重写循环。
- **根因**：`reviewer` 拼参考依据时只用了 `hits`，没拼 `tool_result`——审查者的"参考依据"和回答者的"输入"不一致。
- **修复**：`refs` 拼上 `[工具结果] tool_result`（截断 500 字）。
- **教训**：**审查者的输入必须 ≥ 回答者的输入**，否则审查失真，会误杀正确回答。

### 9. tool_agent 每次提问都触发一次 LLM 判断（⚠️ 已知限制）

- **现象**：`graph` 中 `retriever → tool_agent → answerer` 是固定边，**即使检索已命中**，tool_agent 也会再问一次 LLM"要不要用工具"。
- **影响**：每次提问多一次 LLM 调用，增加延迟和成本。
- **修法（未来）**：检索命中的问题直接跳过工具判断；只在弱相关/无结果/需要计算时走工具。
- **教训**：固定编排的图里，"每步都跑"和"按需跑"是有成本的差别；等数据量上来后值得加条件边。

### 10. execute_python 的 wrapper 不能用 f-string 拼接（✅ 教训沉淀）

- **现象/根因**：`execute_python` 把一段 wrapper 源码传给子进程执行。如果父进程用 **f-string** 拼接 wrapper，`type(e).__name__` 会在**父进程**被提前求值（父进程作用域里没有 `e`），子进程反而拿不到异常类型。
- **修复**：wrapper 用**普通字符串拼接**，把 `type(e).__name__` 留给子进程执行时才求值（代码内注释明确写明原因）。
- **教训**：**跨进程传代码时，"运行时才该求值"的部分绝不能提前展开**。模板拼接要分清"父进程拼"和"子进程算"。

### 11. execute_python 从线程超时改为子进程真超时（✅）

- **现象**：旧实现用线程跑用户代码，超时杀不死失控代码，还可能拖死主程序。
- **修复**（提交 `b58e321`）：`subprocess.run + timeout`，超时 terminate 真能终止；约定退出码：0=正常（含代码异常已转文字）、1=运行期异常、≠0=语法错/崩溃（stderr 有 traceback）。
- **教训**：**执行不可信代码的安全边界按进程隔离，不是线程**；进程内存/锁全隔离，超时才真正可终止。局限要诚实：不是真沙箱，对外开放必须 Docker。

### 12. LLM 空响应 / mock 模式保护（✅）

- **现象**：`chat_with_tools` 可能返回空 choices；mock 模式不支持工具调用。
- **修复**：空响应返回 `None`，调用方统一按"没有工具调用"处理；mock 模式走假回答跑通全流程。
- **教训**：**外部服务的返回永远要防空**，调用方要对"没返回"有默认行为。

---

## 三、会话与记忆

### 13. 会话文件名双 s_ 前缀（✅）

- **现象**：`create()` 返回的 `session_id` 已是 `s_xxx` 风格，但 `_path()` 又拼了一次 `f"s_{safe}.json"` → 实际文件名变成 `s_s_xxx.json`，和 sid 不一致。模块内部 `_load/_save` 都走 `_path` 所以**碰巧自洽**，但任何"按 sid 直觉拼文件"的外部代码（迁移/备份/CLI）都会找不到文件。
- **修复**：`_path()` 改为 `f"{safe}.json"`；`list_sessions()` 的 glob 同步从 `s_*.json` 改为 `*.json`。
- **教训**：**"碰巧自洽"的代码是隐藏地雷**。ID 生成和路径拼接各管一段前缀，两边约定不一致时不会立刻报错，只在被外部调用时爆。

### 14. 会话文件命名变更无兼容 → 补一次性迁移（✅）

- **现象**：历史上产生过 `s_s_xxx.json` 旧文件：列表能列出（读的是文件内 id），但按 sid 找不到文件、历史读不到。
- **修复**：`_migrate_legacy()` 按**文件内记录的 id** 重命名文件（幂等，进程内只跑一次；`sessions/*.json` 全扫，异常跳过）。
- **教训**：**改存储格式必须带一次性迁移**。宁可多写迁移代码，也不能让旧数据静默丢失。

### 15. 长期记忆 memory.py 是孤岛（⚠️ 未接线）

- **现象**：`kb/memory.py` 数据层完整（profile.json 读写/去重/截断/回灌文本），但**没有任何业务代码引用它**：没接"提炼用户画像"、没接"问答前回灌"，`knowledge/memory/profile.json` 从未生成。
- **影响**：功能列表上有"长期记忆"，实际问答流程里从未生效（短期会话记忆已生效，跨会话长期记忆是空头支票）。
- **修法（未来）**：在 graph/main 中接入——提问前把 `memory.to_text()` 拼进 system prompt；回答后异步提炼新事实并 `memory.save()`。
- **教训**：**数据层写完 ≠ 功能完成**。新模块要"接线"进主流程才算数，否则就是死代码，面试被问穿会很尴尬。

---

## 四、Web 与前端

### 16. Windows GBK 控制台 emoji 崩溃（🟡 设计约定）

- **现象**：`print("📁")` 在 Windows GBK 控制台抛 `UnicodeEncodeError`，可能导致 API 500。
- **约定**：代码里 `print` 一律用纯文字 `[标记]`，禁止 emoji（风险表 #1，代码注释里也有此约定）。
- **教训**：**Windows 控制台编码是真实坑**；日志输出保持 ASCII/纯文字最稳。

### 17. 前端脚本语法错误曾致整页失效（✅ 已修复）

- **现象**：`static/index.html` 曾有脚本语法错误，浏览器直接报错，**所有交互失效**（上传/提问/会话全不能用）。
- **修复**：静态语法修复；验证方式：提取 `<script>` 内容用 `node --check` 校验。
- **教训**：**前端改动后必须做语法/回归验证**。肉眼看着对 ≠ 浏览器不报错；改完一定要在真实浏览器/至少语法检查里过一遍。

### 18. chromadb telemetry 噪音（🟡 已处理）

- **现象**：chromadb 遥测在脚本/自测时打噪音报错。
- **处理**：脚本里设 `$env:ANONYMIZED_TELEMETRY="False"`；不影响功能。
- **教训**：第三方库的遥测噪音是环境级小坑，记一笔免得每次困惑。

---

## 五、依赖与工程

### 19. httpx>=0.28 与 openai 1.30 不兼容（✅）

- **现象**：openai 客户端崩溃。
- **根因**：httpx 0.28 移除了 `proxies` 参数，openai 1.30 不兼容。
- **修复**（提交 `d565d23`）：`requirements.txt` 锁 `httpx<0.28`，并**在注释里写明原因**。
- **教训**：**版本锁必须带原因注释**，否则后人看到锁版本会以为是多余的、删掉又崩。

### 20. 上传文件名/主题未净化可路径穿越（✅）

- **现象（潜在）**：`/api/review` 的 filename、topic 如果直接拼 `Path` 用于 move/unlink，构造 `../` 或绝对路径可越界读写。
- **修复**：`pending.py` 加 `_check_filename`（拒绝 `/`、`\`、`.`、`..`、任何路径成分）和 `_check_topic`（只允许单层目录名，拒绝 `raw/..`）。
- **教训**：**任何"用户输入 → 拼路径"的地方都要白名单净化**，这是本地个人工具也必须守的底线。

### 21. 待审查文件必须放 raw 外（🟡 设计）

- **现象（潜在）**：如果 pending 文件放在 `raw/` 里，`ingest` 递归扫描会**误入库**未审查的文件。
- **设计**：`pending/` 独立在 `knowledge/pending/`，与 raw 物理隔离（风险表 #6 的同类思路）。
- **教训**：**扫描器递归目录时，待处理区要物理隔离**，不能靠"约定不往里放"。

---

## 六、仍未做 / 已知缺口（关联现状）

开发记录不只记过去的坑，也记当前欠账（2026-09-04 检查结论）：

| 缺口 | 类型 | 说明 |
|---|---|---|
| 评估体系空白 | 核心缺口 | 0 条标注集、无 eval 脚本；0.6 阈值/top_k/分块参数全是经验值，无法度量检索质量 |
| 长期记忆未接线 | 核心缺口 | 见坑 #15，memory.py 孤岛 |
| 零回归测试 | 核心缺口 | 0 个 pytest；增量/去重/阈值/会话隔离全凭手动验证 |
| splitter 超长段 | 待办小修 | 见坑 #4 |
| tool_agent 每问必跑 | 待办小修 | 见坑 #9 |
| git 工作区未整理 | 待办小修 | 15 个文件改动 + 3 个新文件未提交；台账进 git 见坑 #3 |
| 阶段 6-8（compiled_wiki / 有监督标签 / 多知识库） | 预留 | 等数据量/样本够了再开 |

---

## 七、原则沉淀（这些坑归纳出的规律）

1. **存储标识必须全局唯一**——source 用相对路径，别用文件名（坑 #2）。
2. **hash 是内容的身份证**——去重、增量、删除检测全靠它，按内容不按文件名（坑 #3 关联设计）。
3. **原始与衍生分离**——`raw/` 是唯一真源，向量库/台账/缓存都是可删可重建的衍生数据。
4. **阈值双保险**——retriever 判一次，`ask()` 返回前再判一次（坑 #6）。
5. **审查者输入 ≥ 回答者输入**——审查依据缺失会导致误杀（坑 #8）。
6. **合并结果显式排序**——多来源合并后必须 sort，别信"第一个"（坑 #6）。
7. **跨进程传代码，模板别提前求值**（坑 #10）。
8. **执行不可信代码按进程隔离**，不是线程（坑 #11）。
9. **改存储格式必带迁移**，宁可多写代码别丢数据（坑 #14）。
10. **数据层写完 ≠ 功能完成**，接进主流程才算数（坑 #15）。
11. **版本锁带原因注释**，防止后人误删（坑 #19）。
12. **用户输入拼路径必须净化**（坑 #20）。
