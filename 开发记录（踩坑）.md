# 🕳️ kb-v2 开发记录（踩坑）

> 本文件记录 kb-v2 从零开发至今踩过的每一个坑：**现象 → 根因 → 修复 → 教训**。
> 时间范围：2026-08-31（首个 commit `4add43a`）→ 2026-09-05，共 30+ 个 commit。
> 状态约定：✅ 已修复 ｜ ⚠️ 已知限制（未修）｜ 🟡 设计约定（规避型）
>
> 素材来源：git 提交历史、代码内修复注释、检查记录、项目计划书风险表。全部如实，不补写没发生的事。

---

## 〇、快速索引

| 分类 | 坑数 | 关键坑 |
|---|---|---|
| 存储与数据层 | 4 | 分块超 embedding 上限 / 同名文件互删 / 台账进 git |
| Agent 与 LLM 层 | 8 | 误导入 / 合并未排序 / 工具结果被丢 / 审查依据缺失 |
| 会话与记忆 | 3 | 双 s_ 前缀 / 无兼容迁移 / 记忆孤岛 |
| Web 与前端 | 6 | GBK emoji 崩溃 / 前端脚本语法错 / chroma 遥测 / 表格渲染缺失 / 流式多线程回调 / 左侧边栏改造 |
| 依赖与工程 | 3 | httpx 版本锁 / 路径穿越 / pending 误扫 |
| 标签自治体系 | 3 | 0.85 阈值 0% 命中 / 跨领域误匹配 / 模块级循环依赖 |
| 环境与部署 | 2 | torch import 卡死 / .git ACL 锁定 |

---

## 一、存储与数据层

### 1. 分块大小超过 Embedding 模型上限（✅）

- **现象**：入库后向量化结果被截断，检索质量受影响。
- **根因**：`chunk_size` 沿用旧项目经验值 750 字，但 bge-small-zh-v1.5 的编码上限是 **512 token**。配置注释后来写明"中文约 1 字 ≈ 1 token"，750 字明显超限，超长部分 embedding 直接被截。
- **修复**：`kb_config.yaml` 中 `chunk_size: 750 → 400`（400 字 ≈ 400 token，给模型留足余量）。
- **教训**：Embedding 模型的 token 上限和 LLM 的 token 上限是两回事；任何模型相关参数都要对着模型规格核对，不能照搬旧项目的经验值。

### 2. 不同目录同名文件互相覆盖 / 误删（✅）

- **现象**：`topicA/同名.md` 和 `topicB/同名.md` 同时存在时，删除/重导会互相删片段。
- **根因**：向量库 metadata 的 `source` 字段只存文件名（如 `同名.md`），**全局不唯一**——按 source 删除时两个文件会被当成一个。
- **修复**（提交 `3ded77f`）：
  - `source` 统一改为**相对路径**（`topic/.../文件名`），全局唯一；
  - `vector_store.delete_by_source` 增加 `topic` 参数，兼容旧格式数据清理；
  - `pipeline` 重导前按 `(source, topic)` 去重，每个文件只删一次，不再对每个 chunk 重复调删除。
- **教训**：存储层的标识必须**全局唯一**。"当前只有几个文件、看着够用"的标识，会在多目录场景爆雷。

### 3. 台账 doc_index.json 被 git 跟踪（⚠️ 未处理）

- **现象**：每次 `ingest` 后 `git status` 都显示 `knowledge/index/doc_index.json` modified。
- **根因**：`.gitignore` 忽略了 `vector_store/`、`chunks/`，却漏了 `index/`。
- **建议**：把 `knowledge/index/` 加入 `.gitignore`——台账是从 raw 算出来的衍生数据，可随时重建，不该进版本库制造噪音。
- **教训**：运行时自动生成的**数据文件**一律 gitignore，代码和衍生数据分开。

### 4. 单段落超长不切分（⚠️ 已知限制）

- **现象**：`splitter.chunk_md` 中，若**单个段落**本身超过 `chunk_size`（400），且当前缓冲区为空时，整段直接放入不切分。
- **影响**：罕见（中文笔记里超 400 字的单段落不常见），但会产生超长片段，影响检索精度。
- **修法（未来）**：段落内部再按句/字符二次切分，或用滑动窗口兜底。
- **教训**：分块器对"超长不可分割单元"要有兜底策略，不能假设输入永远规整。

---

## 二、Agent 与 LLM 层

### 5. IDE 自动补全事故：误导入 typer.cli.state（✅）

- **现象**：`kb/agents/graph.py` 第一行 `from typer.cli import state`，全文件无人使用。
- **根因**：IDE 自动补全手滑引入的死代码；靠项目里间接依赖（typer）恰好能跑，没立刻暴露。
- **修复**（提交 `3ded77f`）：删除该导入。
- **教训**：**未使用的 import 也是评审点**。"碰巧能跑"不意味着代码是对的；提交前扫一眼 diff 里的多余行。

### 6. 多查询合并后未排序，阈值误判（✅）

- **现象**：Planner 拆多个查询 → Retriever 合并去重后，用 `hits[0]["score"] < threshold` 判断是否转联网，但合并后的列表**不是按分数排序**的，`hits[0]` 可能是低分项 → 检索明明命中也误判为"弱相关"转联网。
- **修复**（提交 `3ded77f`）：
  - 合并后显式 `hits.sort(key=score, reverse=True)`；
  - `ask()` 返回前用 `max(h["score"])` 再做一次阈值校验（**双保险**，旧项目踩过"乱来源"的坑）。
- **教训**：**跨多个来源合并的结果必须显式排序**；阈值判断要基于全局最优，不是位置上的第一个。

### 7. 检索落空时把工具结果丢了（✅）

- **现象**："用 Python 算 2 的 10 次方"这类问题，Retriever 在知识库检索落空 → Answerer 走"无资料"分支提前 return，**工具已经调出来的结果 tool_result 被丢掉**，回答退化成"凭常识 1024"。
- **根因**：`answerer` 判断"有没有料"只看了 `hits`，没算 `tool_result`。而工具结果恰恰在**检索落空时最有用**。
- **修复**：`has_material = state.get("hits") or state.get("tool_result")`。
- **教训**：判断"有没有可用的料"要看**所有信息来源**，不能只盯检索这一路；流程节点间的数据传递要检查"谁可能在前一步拿到东西"。

### 8. Reviewer 审查依据不含工具结果（✅）

- **现象**：工具型答案（计算/联网）被 Reviewer 拿"空参考"审查 → 误判没依据 → 打回重写循环。
- **根因**：`reviewer` 拼参考依据时只用了 `hits`，没拼 `tool_result`——审查者的"参考依据"和回答者的"输入"不一致。
- **修复**：`refs` 拼上 `[工具结果] tool_result`（截断 500 字）。
- **教训**：**审查者的输入必须 ≥ 回答者的输入**，否则审查失真，会误杀正确回答。

### 9. tool_agent 每次提问都触发一次 LLM 判断（⚠️ 已知限制）

- **现象**：`graph` 中 `retriever → tool_agent → answerer` 是固定边，**即使检索已命中**，tool_agent 也会再问一次 LLM"要不要用工具"。
- **影响**：每次提问多一次 LLM 调用，增加延迟和成本。
- **修法（未来）**：检索命中的问题直接跳过工具判断；只在弱相关/无结果/需要计算时走工具。
- **教训**：固定编排的图里，"每步都跑"和"按需跑"是有成本的差别；等数据量上来后值得加条件边。

### 10. execute_python 的 wrapper 不能用 f-string 拼接（✅ 教训沉淀）

- **现象/根因**：`execute_python` 把一段 wrapper 源码传给子进程执行。如果父进程用 **f-string** 拼接 wrapper，`type(e).__name__` 会在**父进程**被提前求值（父进程作用域里没有 `e`），子进程反而拿不到异常类型。
- **修复**：wrapper 用**普通字符串拼接**，把 `type(e).__name__` 留给子进程执行时才求值（代码内注释明确写明原因）。
- **教训**：**跨进程传代码时，"运行时才该求值"的部分绝不能提前展开**。模板拼接要分清"父进程拼"和"子进程算"。

### 11. execute_python 从线程超时改为子进程真超时（✅）

- **现象**：旧实现用线程跑用户代码，超时杀不死失控代码，还可能拖死主程序。
- **修复**（提交 `b58e321`）：`subprocess.run + timeout`，超时 terminate 真能终止；约定退出码：0=正常（含代码异常已转文字）、1=运行期异常、≠0=语法错/崩溃（stderr 有 traceback）。
- **教训**：**执行不可信代码的安全边界按进程隔离，不是线程**；进程内存/锁全隔离，超时才真正可终止。局限要诚实：不是真沙箱，对外开放必须 Docker。

### 12. LLM 空响应 / mock 模式保护（✅）

- **现象**：`chat_with_tools` 可能返回空 choices；mock 模式不支持工具调用。
- **修复**：空响应返回 `None`，调用方统一按"没有工具调用"处理；mock 模式走假回答跑通全流程。
- **教训**：**外部服务的返回永远要防空**，调用方要对"没返回"有默认行为。

---

## 三、会话与记忆

### 13. 会话文件名双 s_ 前缀（✅）

- **现象**：`create()` 返回的 `session_id` 已是 `s_xxx` 风格，但 `_path()` 又拼了一次 `f"s_{safe}.json"` → 实际文件名变成 `s_s_xxx.json`，和 sid 不一致。模块内部 `_load/_save` 都走 `_path` 所以**碰巧自洽**，但任何"按 sid 直觉拼文件"的外部代码（迁移/备份/CLI）都会找不到文件。
- **修复**：`_path()` 改为 `f"{safe}.json"`；`list_sessions()` 的 glob 同步从 `s_*.json` 改为 `*.json`。
- **教训**：**"碰巧自洽"的代码是隐藏地雷**。ID 生成和路径拼接各管一段前缀，两边约定不一致时不会立刻报错，只在被外部调用时爆。

### 14. 会话文件命名变更无兼容 → 补一次性迁移（✅）

- **现象**：历史上产生过 `s_s_xxx.json` 旧文件：列表能列出（读的是文件内 id），但按 sid 找不到文件、历史读不到。
- **修复**：`_migrate_legacy()` 按**文件内记录的 id** 重命名文件（幂等，进程内只跑一次；`sessions/*.json` 全扫，异常跳过）。
- **教训**：**改存储格式必须带一次性迁移**。宁可多写迁移代码，也不能让旧数据静默丢失。

### 15. 长期记忆 memory.py 是孤岛（⚠️ 未接线）

- **现象**：`kb/memory.py` 数据层完整（profile.json 读写/去重/截断/回灌文本），但**没有任何业务代码引用它**：没接"提炼用户画像"、没接"问答前回灌"，`knowledge/memory/profile.json` 从未生成。
- **影响**：功能列表上有"长期记忆"，实际问答流程里从未生效（短期会话记忆已生效，跨会话长期记忆是空头支票）。
- **修法（未来）**：在 graph/main 中接入——提问前把 `memory.to_text()` 拼进 system prompt；回答后异步提炼新事实并 `memory.save()`。
- **教训**：**数据层写完 ≠ 功能完成**。新模块要"接线"进主流程才算数，否则就是死代码，面试被问穿会很尴尬。

---

## 四、Web 与前端

### 16. Windows GBK 控制台 emoji 崩溃（🟡 设计约定）

- **现象**：`print("📁")` 在 Windows GBK 控制台抛 `UnicodeEncodeError`，可能导致 API 500。
- **约定**：代码里 `print` 一律用纯文字 `[标记]`，禁止 emoji（风险表 #1，代码注释里也有此约定）。
- **教训**：**Windows 控制台编码是真实坑**；日志输出保持 ASCII/纯文字最稳。

### 17. 前端脚本语法错误曾致整页失效（✅ 已修复）

- **现象**：`static/index.html` 曾有脚本语法错误，浏览器直接报错，**所有交互失效**（上传/提问/会话全不能用）。
- **修复**：静态语法修复；验证方式：提取 `<script>` 内容用 `node --check` 校验。
- **教训**：**前端改动后必须做语法/回归验证**。肉眼看着对 ≠ 浏览器不报错；改完一定要在真实浏览器/至少语法检查里过一遍。

### 18. chromadb telemetry 噪音（🟡 已处理）

- **现象**：chromadb 遥测在脚本/自测时打噪音报错。
- **处理**：脚本里设 `$env:ANONYMIZED_TELEMETRY="False"`；不影响功能。
- **教训**：第三方库的遥测噪音是环境级小坑，记一笔免得每次困惑。

### 19. 回答里的 Markdown 表格显示为原始文本（✅ 已修复）

- **现象**：LLM 回答里的 `| 列1 | 列2 |` 表格在前端直接显示为原始竖线文本，没有渲染成 `<table>`。
- **根因**：`renderMarkdown()` 只处理了标题、加粗、行内码、列表、代码块，**漏了表格语法**。
- **修复**：`renderMarkdown()` 增加表格正则——连续 `|` 分隔行 → 解析表头/分隔行/数据行 → 输出 `<table class="md-table">`，并加对应 CSS（表头蓝底、斑马纹）。
- **教训**：**轻量 Markdown 渲染器要列全语法清单**，不能只做"常用的几个"；表格是 LLM 回答里的高频格式，漏了体验差很多。

### 20. SSE 流式输出的多线程回调实现（✅ 已修复，设计沉淀）

- **需求**：前端要打字机效果，后端 LangGraph 是同步 `graph.stream(state)` 逐节点执行，answerer 节点内部调 LLM 生成文本——**怎么把 LLM 的增量 token 实时推到 SSE 流？**
- **坑**：`graph.stream()` 只能拿到节点级状态（节点开始/结束），拿不到 answerer 内部 LLM 的增量。如果等 answerer 节点结束才推文本，前端就是"等 10 秒然后一次性蹦出全文"，不是流式。
- **修复**（`kb/agents/graph.py`）：
  - `_stream_local = threading.local()` 挂载当前线程的 delta 回调函数；
  - `_chat_stream(system, user)` 流式调 LLM，每拿到一个增量 token 就**同时**拼进返回文本 + 调 `_get_delta_cb()(token)` 推回调；
  - answerer 节点按"当前线程有无回调"二选一：有回调走 `_chat_stream`，无回调走原 `chat`；
  - 文件尾追加 `ask_stream(question, history)` **生成器**：后台线程跑 `graph.stream(state)`，逐节点 emit `{"type":"status","stage":...}`，answerer 增量经 threading.local 回调进 queue，主线程循环消费 queue 实时 yield；
  - `main.py` 新增 `POST /api/chat/stream`，SSE 格式 `data: {json}\n\n`，结束后落库会话历史 + memory 提炼（对齐 `/api/chat` 的副作用）。
- **实测**：SSE 200，5 个阶段 status（规划问题→检索笔记→调用工具→生成回答→审查回答），328 次 delta 增量，首 delta 9.2s，打字机效果正常。
- **教训**：**同步图引擎 + 流式 LLM 的桥接要靠线程局部变量 + 回调 + 队列**；节点级事件和 token 级增量是两个粒度，要分别处理。threading.local 是关键——保证多请求并发时回调不串台。

### 21. CRLF 行尾导致 Edit 工具匹配失败（🟡 设计约定）

- **现象**：用 Edit 工具修改 `main.py` 和 `kb/*.py` 时，`old_string` 明明和文件内容看起来一样，却报"not found"。
- **根因**：这些 Python 文件是 **CRLF（\r\n）行尾**，而 Edit 工具的匹配按 LF（\n）处理，`\r` 导致精确匹配失败。`static/index.html` 是 UTF-8 无 BOM + LF，Edit 正常。
- **修复/约定**：修改 CRLF 文件时，**必须用 Python 脚本 `io.open(path, encoding="utf-8", newline="")` 读写**，精确控制行尾；不能用 Edit 工具。插入代码时用字符串替换 + marker 定位。
- **教训**：**跨平台项目要统一行尾**，或至少在文档里记清楚哪些文件是 CRLF；工具链对行尾敏感时，选错工具会浪费大量时间。

---

## 五、依赖与工程

### 19. httpx>=0.28 与 openai 1.30 不兼容（✅）

- **现象**：openai 客户端崩溃。
- **根因**：httpx 0.28 移除了 `proxies` 参数，openai 1.30 不兼容。
- **修复**（提交 `d565d23`）：`requirements.txt` 锁 `httpx<0.28`，并**在注释里写明原因**。
- **教训**：**版本锁必须带原因注释**，否则后人看到锁版本会以为是多余的、删掉又崩。

### 20. 上传文件名/主题未净化可路径穿越（✅）

- **现象（潜在）**：`/api/review` 的 filename、topic 如果直接拼 `Path` 用于 move/unlink，构造 `../` 或绝对路径可越界读写。
- **修复**：`pending.py` 加 `_check_filename`（拒绝 `/`、`\`、`.`、`..`、任何路径成分）和 `_check_topic`（只允许单层目录名，拒绝 `raw/..`）。
- **教训**：**任何"用户输入 → 拼路径"的地方都要白名单净化**，这是本地个人工具也必须守的底线。

### 21. 待审查文件必须放 raw 外（🟡 设计）

- **现象（潜在）**：如果 pending 文件放在 `raw/` 里，`ingest` 递归扫描会**误入库**未审查的文件。
- **设计**：`pending/` 独立在 `knowledge/pending/`，与 raw 物理隔离（风险表 #6 的同类思路）。
- **教训**：**扫描器递归目录时，待处理区要物理隔离**，不能靠"约定不往里放"。

---

## 六、标签自治体系

### 22. 用户建议的 0.85 标签相似度阈值实测 0% 命中率（✅ 已校准为三分层）

- **现象**：用户提出标签自治方案——"新笔记生成向量去标签库检索 Top3，最高相似度超 0.85 就复用标签"。实测 101 篇文档的 Top1 相似度分布：min=0.467 / p50=0.597 / max=0.741，**0.85 阈值命中率 0%**——方案完全失效。
- **根因**：bge-small-zh-v1.5 的余弦相似度在"短标签 vs 长文档"场景下，分数天然偏低（标签只有几个字，文档有几百字，语义空间距离大）。0.85 是"句子对句子"的阈值，不适合"标签对文档"。
- **修复**（`scripts/tag_sim_probe.py` 实测 101 篇×35 标签后校准）：
  - 阈值改为**三分层**：`≥0.60` 直接复用（免 LLM）｜`0.45~0.60` LLM 确认（候选给 Top5）｜`<0.45` LLM 生成新标签并写回标签库；
  - 实测命中率：0.60→46.5%、0.55→80.2%、0.50→97.0%；
  - `kb/ingestion/tagger.py` 重构为 v2 主流程，`kb/ingestion/tag_store.py` 新增标签向量库（chroma tags 集合）。
- **实测闭环**：W01-React 入门文档 Top1=0.558 → LLM 确认路径 → 新标签 `React` 自动写回标签库（38→40），自治闭环生效。
- **教训**：**阈值不能拍脑袋，必须用真实数据分布校准**；用户的直觉阈值（0.85）在不同语义场景下可能完全失效。"先跑数据分布再定阈值"是铁律。

### 23. tag_store ↔ tagger 模块级循环依赖（✅ 已解决）

- **现象**：`tag_store.py` 需要调用 `tagger._clean_tag()` 清洗标签，`tagger.py` 需要调用 `tag_store.retrieve/add_tags` 做标签检索——**模块级互相 import，Python 导入时死循环**。
- **修复**：
  - `tag_store.py` 的 `_clean_tag()` 改为**函数内延迟导入**（`from .tagger import _clean_tag as _ct`），不在模块顶层 import；
  - `tag_store._seed_tags()` 内需要 `tagger._DEFAULT_SCHEMA` 也改为延迟导入；
  - `tagger.py` 顶层正常 import `tag_store`（因为 tag_store 顶层不 import tagger，循环被打破）。
- **教训**：**循环依赖的标准解法是"延迟导入 + 单向依赖"**——被依赖方不在顶层 import 依赖方，只在函数内部按需 import。模块设计时就要画依赖图，别等跑起来才发现循环。

### 24. 标签跨领域误匹配（⚠️ 观察项，可能需上调复用线）

- **现象**：ES6 入门文档的 Top1 标签匹配到"Python基础"（相似度 0.60），触发复用路径——但 ES6 和 Python 是完全不同的领域，属于误匹配。
- **根因**：0.60 复用线在"通用编程概念"上容易跨领域撞车（"变量""函数""循环"等基础概念在 Python 和 JS 里都有，向量距离近）。
- **影响**：低概率（实测 101 篇里少数几篇），但会导致标签污染——JS 文档被打上 Python 标签。
- **建议**：观察一段时间，如果误匹配率高，把复用线从 0.60 上调到 0.65（更多文档走 LLM 确认路径，减少误复用）。
- **教训**：**阈值校准要看"误报率"，不能只看"命中率"**——0.60 命中率 46.5% 看起来不错，但如果其中 10% 是跨领域误匹配，实际有效复用率就只有 42%。阈值是精确率和召回率的权衡。

---

## 七、环境与部署

### 25. torch import 卡死超过 3 分钟，服务无法启动（⚠️ 未解决，环境问题）

- **现象**：2026-09-05 重启服务时，`import torch`（sentence_transformers 依赖）卡死超过 3 分钟无响应，CPU 模式和 CUDA 模式都卡住，导致 uvicorn 服务起不来（浏览器显示 chrome-error）。
- **排查**：`import chromadb` 只要 1.2 秒；`import sentence_transformers` 和 `import torch` 均卡死；设置 `CUDA_VISIBLE_DEVICES=""` 强制 CPU 也卡死。
- **可能原因**：①杀毒软件（Windows Defender）扫描 torch DLL 导致极慢；②torch 安装损坏；③磁盘 IO 极慢；④torch 缓存损坏。**之前服务能正常启动，说明 torch 本身是好的，是环境临时异常**。
- **临时绕过方案**：修改 `kb/embedding.py` 强制走 fallback 哈希向量器（不加载 sentence_transformers），服务能秒起，但检索质量下降。
- **教训**：**深度学习依赖（torch/sentence_transformers）是启动慢的重灾区**，第一次 import 可能需要几分钟（加载 DLL、初始化）；生产环境要做"启动健康检查 + 超时告警"，别等用户报"页面打不开"才发现。个人项目也要记一笔：torch import 慢 ≠ 代码坏了。

### 26. .git 目录被安全软件 ACL 锁定（✅ 已自行解决）

- **现象**：`git commit` 时报错，`.git` 目录被安全软件设置了 ACL（Deny Write + ReadOnly），无法写入。
- **根因**：安全软件（如 360/Windows Defender）误判 `.git` 目录为风险，锁定了写入权限。
- **修复**：由环境/用户自行解决（调整安全软件设置或恢复 ACL）。
- **教训**：**Windows 上的安全软件是 git 操作的隐形杀手**——遇到 git 权限报错，先查 `.git` 目录的 ACL，别以为是 git 本身坏了。

---

## 八、仍未做 / 已知缺口（关联现状）

开发记录不只记过去的坑，也记当前欠账（2026-09-05 检查结论）：

| 缺口 | 类型 | 说明 |
|---|---|---|
| 管理页面未完成 | 核心缺口 | 后端 `/api/manage/stats`、`/api/docs`、`/api/tags` 已写，但前端 `manage.html` 管理页面未做（文档管理/标签管理/导出中心/系统统计四个 Tab） |
| 知识库快照导出 | 核心缺口 | 无法导出 chroma 向量库 + 标签库 + 台账为可迁移备份；换机器需全量重建 |
| 历史会话完整导出 | 待办 | 当前只能导出页面上显示的消息；切换会话后历史消息未从后端加载（虽有 `/api/sessions/{id}/history` 接口但前端未调用） |
| 标签治理前端 | 待办 | 标签自治后端已完成（90 标签、三分层阈值、自动写回），但前端没有标签管理面板（查看/合并/删除标签） |
| 异常与错误处理闭环 | 待办 | SSE 流式接口异常断开、超大文件上传、chroma 锁冲突等缺少友好降级 |
| 配置化管理 | 待办 | 标签阈值（0.60/0.45）、SSE 超时、top_k 等硬编码在代码里，没有统一 config |
| 评估体系 | 长期 | 23 条检索用例已建（`tests/retrieval_cases.json`），但无自动化 eval 脚本；阈值/top_k 仍是经验值 |
| splitter 超长段 | 待办小修 | 见坑 #4 |
| tool_agent 每问必跑 | 待办小修 | 见坑 #9 |
| torch import 慢 | 环境问题 | 见坑 #25，服务启动需 3+ 分钟（临时） |
| 阶段 6-8（compiled_wiki / 有监督标签 / 多知识库） | 预留 | 等数据量/样本够了再开 |

### 已完成的里程碑（2026-09-05 更新）

- ✅ **标签自治体系**：标签向量库（chroma tags 集合，90 标签）+ 三分层阈值（0.60/0.45）+ 新标签自动写回闭环，实测三种路径（复用/LLM确认/生成新标签）均生效
- ✅ **长期记忆接线**：memory.py 已接 graph/single/main，17 个测试用例通过
- ✅ **pytest 50/50 通过**（19.83s），含 test_memory 17 用例与 tag_store 相关
- ✅ **SSE 流式输出**：后端 llm.chat_stream + graph.ask_stream + main /api/chat/stream，实测 5 阶段 status + 328 delta
- ✅ **前端左侧边栏布局**：豆包风格左栏（会话列表 + 知识库操作 + 统计）+ 主区对话，浏览器实测通过
- ✅ **会话导出**：顶栏导出按钮，一键下载当前会话为 Markdown
- ✅ **Markdown 表格渲染**：回答里的表格正常渲染为 `<table>`
- ✅ **代码块复制按钮**：回答里的代码块右上角复制按钮
- ✅ **全局 Toast 提示**：上传/入库/会话操作统一 Toast

---

## 九、原则沉淀（这些坑归纳出的规律）

1. **存储标识必须全局唯一**——source 用相对路径，别用文件名（坑 #2）。
2. **hash 是内容的身份证**——去重、增量、删除检测全靠它，按内容不按文件名（坑 #3 关联设计）。
3. **原始与衍生分离**——`raw/` 是唯一真源，向量库/台账/缓存都是可删可重建的衍生数据。
4. **阈值双保险**——retriever 判一次，`ask()` 返回前再判一次（坑 #6）。
5. **审查者输入 ≥ 回答者输入**——审查依据缺失会导致误杀（坑 #8）。
6. **合并结果显式排序**——多来源合并后必须 sort，别信"第一个"（坑 #6）。
7. **跨进程传代码，模板别提前求值**（坑 #10）。
8. **执行不可信代码按进程隔离**，不是线程（坑 #11）。
9. **改存储格式必带迁移**，宁可多写代码别丢数据（坑 #14）。
10. **数据层写完 ≠ 功能完成**，接进主流程才算数（坑 #15）。
11. **版本锁带原因注释**，防止后人误删（坑 #19）。
12. **用户输入拼路径必须净化**（坑 #20）。
13. **阈值不能拍脑袋，必须用真实数据分布校准**——用户直觉阈值 0.85 在标签场景 0% 命中，实测后改为 0.60/0.45 三分层（坑 #22）。
14. **循环依赖靠延迟导入 + 单向依赖打破**——被依赖方不在顶层 import 依赖方（坑 #23）。
15. **阈值校准要看误报率，不只看命中率**——0.60 命中率 46.5% 但有跨领域误匹配，精确率和召回率要权衡（坑 #24）。
16. **同步图引擎 + 流式 LLM 的桥接靠 threading.local + 回调 + 队列**——节点级事件和 token 级增量是两个粒度，分别处理（坑 #20）。
17. **CRLF 文件不能用 Edit 工具，必须用 Python 脚本 io.open(newline="")**——行尾敏感时选错工具浪费大量时间（坑 #21）。
18. **深度学习依赖是启动慢的重灾区**——torch import 可能卡死几分钟，生产环境要做启动健康检查（坑 #25）。
