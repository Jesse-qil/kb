# 🧭 读懂 kb-v2 个人知识库（从头到尾）

> 这份文档的目标：让一个**没看过代码的人**，顺着读一遍就能理解这个项目
> 「为什么这么设计、每个文件干什么、一条数据怎么从头走到尾」。
> 读完它再读代码，代码会"顺"起来。

---

## 第 0 章：一句话说清项目

> **kb-v2 是一个"个人知识库 + AI 助手"**：你把笔记（.md / .docx / .pdf）丢进去，
> 系统自动**解析、分类、向量化入库**，之后你可以像聊天一样问它，它从你的笔记里
> 找答案回答你——还带"多 Agent 思考"和"会话记忆"。

拆成三个词就懂了：

| 词 | 意思 |
|---|---|
| **知识库** | 存你的笔记，能按"相似度"检索 |
| **RAG** | Retrieval-Augmented Generation：检索增强生成——先查资料，再让大模型基于资料回答（防止它瞎编）|
| **Agent** | 会"想"的问答流程：规划 → 检索 → 回答 → 审查，像个小团队 |

---

## 第 1 章：先跑起来（建立体感）

```powershell
cd D:\Python\Agent\kb-v2
# 1. 启动 Web（浏览器打开 http://127.0.0.1:8000）
.\venv\Scripts\python.exe -m uvicorn main:app --reload

# 2. 或者用命令行
.\venv\Scripts\python.exe -m kb.cli scan      # 看笔记有哪些变化
.\venv\Scripts\python.exe -m kb.cli ingest    # 把笔记入库
.\venv\Scripts\python.exe -m kb.cli query "装饰器是什么"   # 检索
.\venv\Scripts\python.exe -m kb.cli ask "装饰器是什么"     # 问答
```

打开网页后你可以：
- **问**"库里有什么？" → 走"总览"列出目录
- **问**"装饰器是什么？" → 检索 → 回答 → 显示参考来源
- **点右上角"上传笔记"** → 传文件 → 进"待审查" → 确认入库
- 连续问几句 → 小齐**记得**上文（会话记忆）
- 上传/导入时顶部有**进度条**

先玩 5 分钟，再看下面原理会特别顺。

---

## 第 2 章：全局地图（先看目录结构）

```
kb-v2/
├─ main.py                 ★ Web 入口：FastAPI，所有 HTTP 接口在这
├─ kb_config.yaml          配置（切模型/调参数都在这，不用改代码）
├─ requirements.txt        依赖清单
├─ static/index.html       前端页面（浏览器看到的界面）
│
├─ kb/                     ★★ Python 核心包（代码都在这）
│   ├─ config.py           读配置，给全项目提供路径和参数
│   ├─ llm.py              大模型封装（DeepSeek/OpenAI/Ollama…统一接口）
│   ├─ embedding.py        文本 → 向量（bge 模型，本地免费）
│   ├─ prompts.py          所有"给 AI 看的提示词"集中管理
│   ├─ web_search.py       联网搜索（知识库没有时兜底）
│   ├─ session.py          会话记忆（记住聊过啥）
│   ├─ cli.py              命令行工具
│   ├─ storage/            ★ 存储层：东西怎么存
│   │   ├─ vector_store.py   Chroma 向量库（存片段、按相似度检索）
│   │   └─ doc_index.py      inode 台账（每篇笔记的身份证登记表）
│   ├─ ingestion/          ★ 数据摄入层：笔记怎么进来
│   │   ├─ scanner.py        扫 raw/ 目录，算 hash 识别 增/改/删
│   │   ├─ splitter.py       长文 → 切块
│   │   ├─ pipeline.py       入库编排（串起上面所有步骤）
│   │   ├─ classifier.py     LLM 判断"这篇笔记属于哪个主题"
│   │   └─ pending.py        待审查区管理（上传先搁这，人工确认才入库）
│   └─ agents/             ★ Agent 层：怎么回答问题
│       ├─ single.py         单 Agent：检索 → 拼上下文 → 回答
│       ├─ graph.py          多 Agent：LangGraph 编排（规划→检索→工具→回答→审查）
│       ├─ tools.py          工具注册表（查库/算数/联网）+ Function Calling 循环
│       └─ tool_agent        图里的一个节点：LLM 自主决定要不要调工具（ReAct）
│
└─ knowledge/              ★★★ 数据（运行时自动生成/你手动丢笔记）
    ├─ raw/                你丢原始笔记的地方（.md/.docx/.pdf）
    │   ├─ notes_draft/       草稿区
    │   ├─ reference/         资料区
    │   ├─ project_material/  项目区
    │   └─ AI-Agent/          （LLM 自动分类新建的主题目录）
    ├─ pending/            待审查文件（上传后、确认前）
    ├─ index/doc_index.json 台账（自动）
    ├─ vector_store/chroma/ 向量库（自动）
    └─ chunks/ temp/ export/ ml_data/ compiled_wiki/   （预留）
```

### 三句话记住架构

1. **三层分包**：`ingestion`（进料）→ `storage`（存储）→ `agents`（思考），各管各的。
2. **依赖只能从上往下**：agents 调 storage，storage 不知道 agents 存在。
3. **人和机器分区**：人只碰 `knowledge/raw/`（丢笔记），机器生成的（台账/向量库）放旁边，互不干扰。

---

## 第 3 章：一条笔记的完整旅程（核心！顺着读）

这是整个项目最重要的一张"故事线"，理解了它，代码就顺了。

### 旅程①：手动丢文件（最原始的方式）
```
你把 笔记.md 丢进 knowledge/raw/notes_draft/
        │
        ▼
python -m kb.cli ingest          ← 你敲的命令
        │
        ▼
[ingestion/scanner.py]  扫 raw/，算每个文件的 hash（内容指纹）
        │
        ▼
[storage/doc_index.py]  读旧台账，对比 hash
        │  新文件？改过？没变？删了？  → 分四类
        ▼
[ingestion/splitter.py] 把长文切成 750 字的块（相邻重叠 120 字）
        │
        ▼
[embedding.py]          每块文本 → 向量（512 维数字串）
        │
        ▼
[storage/vector_store.py] 向量 + 原文 一起存进 Chroma
        │
        ▼
[storage/doc_index.py]  更新台账：登记 kb-编号/hash/tags
        │
        ▼
完成！现在可以检索它了
```

### 旅程②：网页上传（带智能分类 + 人工审查）
```
浏览器点"上传笔记"选文件
        │
        ▼
[main.py /api/upload]   收文件
        ├─ 算 md5 → 库里已有相同内容？→ 拒绝（去重）
        ▼
文件存到 knowledge/pending/（待审查区，先不正式入库）
        │
        ▼
[ingestion/classifier.py]  LLM 读内容开头 → 建议主题（如"Python基础"）
        │
        ▼
网页"待审查"面板显示：文件名 + 建议主题
        │  你确认 or 改主题
        ▼
[main.py /api/review]   文件移到 knowledge/raw/<主题>/ → 触发入库
        │
        ▼
（进入旅程①的 ingest，自动向量化入库）
```

**为什么要"待审查"再入库？** 因为 LLM 分类可能错——人工把关一下，分类错了不会污染知识库。你信任它时可以勾"自动入库"跳过这步。

### 旅程③：问一个问题（多 Agent 思考）
```
浏览器输入："装饰器是什么？"
        │
        ▼
[main.py /api/chat]     收问题 + session_id
        ├─ [session.py] 取出这个会话之前聊过的历史
        ▼
[kb/agents/graph.py]    LangGraph 开始跑
        │
        ▼
┌─ Agent1 Planner（规划）────────────────┐
│  让 LLM 判断：这是"库里有什么"类总览问题？│
│  属于哪个主题？把问题拆成几个检索词？     │
└──────────────┬─────────────────────────┘
               │ 是总览问题？→ 直接列目录（Overview 节点）
               ▼ 否则继续
┌─ Agent2 Retriever（检索）──────────────┐
│  每个检索词 → 向量 → Chroma 找相似片段    │
│  最高分 < 0.6（太弱）→ 转联网搜索兜底     │
└──────────────┬─────────────────────────┘
               ▼
┌─ tool_agent（工具判断，ReAct）─────────┐
│  问 LLM：这题要算数/联网吗？            │
│  要 → 调 execute_python/web_search      │
│      结果总结成"人话"存 tool_result      │
│  不要 → tool_result 留空，直接过        │
└──────────────┬─────────────────────────┘
               ▼
┌─ Agent3 Answerer（回答）───────────────┐
│  拼 prompt：之前对话 + 参考资料 + 问题    │
│  让 LLM 基于资料写回答（不瞎编）          │
└──────────────┬─────────────────────────┘
               ▼
┌─ Agent4 Reviewer（审查）───────────────┐
│  检查回答有没有依据、完不完整             │
│  不过关？→ 打回 Answerer 重写（最多3轮）  │
└──────────────┬─────────────────────────┘
               ▼
返回 {answer, sources} → 前端显示 + 参考来源
```

**"多 Agent" ≠ 多个大模型**，是**一个模型分饰四角**，每轮给它不同的角色提示词。好处：拆成小任务更可控、质量可审查。

---

## 第 4 章：关键机制逐个拆解（面试/理解重点）

### 4.1 台账 doc_index.json —— 为什么自己维护一份？

```json
{
  "notes_draft/示例-装饰器.md": {
    "id": "kb-0001",
    "topic": "notes_draft",
    "hash": "208487a1c68eee686c9cf2be8c4d6e0e",
    "mtime": 1788333714,
    "tags": []
  }
}
```

**它解决三个问题**：
1. **增量更新**：hash = 内容指纹。文件没改 → hash 不变 → ingest 直接跳过（不重复向量化，省时间）。
2. **删除检测**：台账里有、raw/ 里没了 → 说明笔记被删 → 自动清掉向量库里它的片段。
3. **稳定编号**：`kb-0001` 仿 inode，同路径沿用不轻易变，方便将来关联。

**为什么不用向量库自己存元数据？** 向量库是"黑盒"，调试麻烦；自建一份 JSON 台账随时能看、能改、能备份。

### 4.2 向量化 + 相似度检索 —— 知识库的核心魔法

| 概念 | 人话 |
|---|---|
| embedding | 把"一段文字"变成"512 个数字"（向量）。意思相近的文字 → 数字也相近 |
| 余弦相似度 | 衡量两个向量方向多接近：1=完全同义，0=无关 |
| top_k | 取最相似的几段（默认 4）|
| score_threshold | 相似度 < 0.6 就当"没找到"——防止拿无关资料硬答 |

流程：**问题 → 向量 → 和库里每段比相似度 → 取最高的几段 → 拼给 LLM**。

### 4.3 为什么要有 0.6 阈值？

没有阈值：问"今天天气"也可能硬搜出笔记里某段，LLM 拿无关资料编答案。
有阈值：搜出来最高才 0.3 → 判定"库里没有" → 转联网 or 明说不知道。
**宁可说不知道，不可瞎编**——这是 RAG 的底线设计。

### 4.4 分块（chunking）—— chunk_size=750, overlap=120

- 长文不能整篇喂给模型（超 token），要切块。
- **overlap（重叠）**：相邻块重叠 120 字，防止一句话被拦腰切断导致意思残缺。
- 检索单位是"块"，回答引用"块"。

### 4.5 LLM 多提供商封装（kb/llm.py）

```
DeepSeek / OpenAI / 硅基流动 / Ollama / mock
      └── 全是 OpenAI 兼容接口 → 一个客户端类搞定
```

- `provider: auto` → 检测 .env 配了哪家 key 用哪家
- 没 key → `mock` 假回答（先跑通流程）
- `chat()` 普通对话；`chat_with_tools()` 带工具注册表

### 4.6 Function Calling / 工具（agents/tools.py）

LLM 天生不会算数、不会查实时信息。工具机制：
```
问"2 的 10 次方"
  → LLM 说：我要调用 execute_python，参数是 print(2**10)
  → 程序执行，把结果 1024 喂回给 LLM
  → LLM 基于结果给出最终回答
```
三个工具：`search_knowledge`（查库）/ `execute_python`（算数，子进程真超时）/ `web_search_tool`（联网）。
在 graph 里，tool_agent 节点只带后两个（检索已由 Retriever 做过）；当检索落空或问题需要计算时，
LLM 自主决定调工具——这就是 ReAct（先想→动手→根据结果再想）。

### 4.7 会话记忆（session.py）—— 我们刚加的

- 浏览器生成一个 `session_id`（存在 localStorage），每次提问带上。
- 后端按 session_id 存最近 6 轮问答，提问时拼进 prompt。
- "🆕 新会话" = 换新 session_id → 记忆清零。
- 注意：这是**短期记忆**（进程内内存，重启服务就没了）；长期记忆/进化还没做。

### 4.8 后台入库 + 进度条

入库最慢的是向量化（几百段要跑模型）。如果同步做，浏览器会一直转圈。
方案：**入库丢进后台线程**跑，前端轮询 `/api/ingest/progress` 拿百分比。

---

## 第 5 章：每个文件怎么读（代码地图）

建议顺序（由底向上，和依赖方向一致）：

1. `kb_config.yaml` + `kb/config.py` —— 先看配置了啥
2. `kb/embedding.py` —— 最短，看怎么把文字变向量
3. `kb/storage/doc_index.py` —— 台账读写（load/save/find_by_hash）
4. `kb/storage/vector_store.py` —— 向量库（query/upsert/delete/count）
5. `kb/ingestion/scanner.py` → `splitter.py` → `pipeline.py` —— 入库三兄弟
6. `kb/ingestion/pending.py` + `classifier.py` —— 上传审查流
7. `kb/llm.py` → `kb/prompts.py` —— AI 调用层
8. `kb/agents/single.py` —— 最简单的问答
9. `kb/agents/graph.py` —— 最复杂，多 Agent 编排（重点啃）
10. `kb/agents/tools.py` + `kb/session.py` —— 工具 + 记忆
11. `main.py` —— 所有 API 端点
12. `static/index.html` —— 前端（看懂会改即可）

每个文件底部基本都有 `if __name__ == "__main__"` 自测，可以直接跑：
```powershell
.\venv\Scripts\python.exe -m kb.cli list
```

---

## 第 6 章：常见疑问（读代码前先看）

**Q：为什么有 single 和 graph 两个问答？**
single = 一条直线（检索→回答），简单教学用；
graph = 带规划/审查的多 Agent，**对外主力**（/api/chat 走它）。
面试可以答："我实现了从单 Agent 到多 Agent 的演进，graph 是生产路径。"

**Q：tools.py 和 graph 里的 tool_agent 什么关系？**
tools.py 是**工具库**（注册表 + 执行函数 + 独立循环）；graph 里的 tool_agent 节点**复用它**——
在流程中问 LLM 要不要调工具，要就用 TOOL_FUNCS 执行。两者已经融合（ReAct）。

**Q：为什么 topic 用目录名，不搞标签？**
现在：目录名（notes_draft、AI-Agent）= 领域，简单够用。
将来：一篇笔记跨多个主题时，才需要 tags 标签补充（doc_index 已留 tags 字段）。

**Q：向量库为什么能删了重建？**
知识库的真源是 `raw/` 里的原始文件。向量库/台账都是从它算出来的**衍生数据**——删了重建（`cli reset` + `ingest`）就能恢复。这就是"原始与衍生分离"设计的好处。

---

## 第 7 章：30 秒面试版总结

> 我做了一个**分层架构的个人知识库**：`storage`（Chroma 向量库 + 自建 inode 台账）、
> `ingestion`（增量入库：hash 指纹检测增改删 + 分块 + 自动分类 + 人工审查）、
> `agents`（LangGraph 多 Agent 问答：规划→检索→回答→审查打回循环，配 Function
> Calling 工具和会话记忆），外层 FastAPI + 前端。核心亮点是**增量更新**（hash 对比
> 跳过未变文件）、**防幻觉阈值**（0.6 相似度）和**上传即入库的自动分类审查流**。

---

*读完这份 → 打开代码从 config 开始按第 5 章顺序读。哪一段读了还不顺，回来对着本章再看。*
