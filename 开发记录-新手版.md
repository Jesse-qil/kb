# 🕳️ 开发记录 · 新手学习清单

> 这是 kb-v2 项目踩过的 **28 个坑**，每条都用大白话讲清楚：**发生了啥 → 为啥会这样 → 怎么解决 → 学到的道理**。
> 学会一条，就把前面的 `[ ]` 勾成 `[x]`（在 Typora / Obsidian / 支持任务列表的编辑器里可以直接点）。
>
> ⚠️ 纯 Markdown 版只能勾选，**没有"自动移到后面"和"删除按钮"**——想要这两个功能，请用同目录下的
> **「开发记录-新手版.html」**（双击用浏览器打开，勾选自动移到最后、可删除、带进度条、关掉不丢）。
>
> 状态说明：✅ 已修复 ｜ ⚠️ 已知限制 ｜ 🔌 没接上 ｜ 🟡 约定规避

---

## 📚 一、存数据相关的坑（存储与数据层）

### 1. 笔记切块太大，超过了向量模型的"记忆上限" ✅
- **发生了啥**：把笔记切成小块存进知识库时，每块本来能切 750 字，但向量模型一次最多只能记住 512 个 token（约 400 多字）。结果每块的后半截都被"腰斩"了，检索时内容不完整。
- **为啥会这样**：分块大小是照着旧项目抄的经验值，没查新用的模型到底能装多少。
- **怎么解决的**：把分块大小从 750 改成 400 字，给模型留足余量（kb_config.yaml）。
- **学到的道理**：换模型就要重新核对参数，不能照抄旧经验；"差不多"会变成数据问题。
- [ ] 我学会了

### 2. 两个文件夹里的同名文件互相"打架" ✅
- **发生了啥**：topicA/同名.md 和 topicB/同名.md 同时存在时，删除其中一篇，另一篇的碎片也被一起删了。
- **为啥会这样**：存数据时只记了文件名（同名.md），没记它在哪个文件夹——就像两个人身份证都写"张三"，根本分不清谁是谁。
- **怎么解决的**：改成用完整路径当"身份证"（topicA/同名.md），删除时按路径精确删，再兼容清理旧数据。
- **学到的道理**：存数据时标识要全局唯一，别只用名字，要带"户口地址"。
- [ ] 我学会了

### 3. 台账文件被 git 盯上了，每次入库都提示"有修改" ⚠️
- **发生了啥**：每次跑完入库，git 就显示台账文件 doc_index.json 变了，其实它只是自动生成的数据，不该进版本库。
- **为啥会这样**：.gitignore 忽略了向量库和缓存目录，唯独漏了台账目录。
- **怎么解决**：把 knowledge/index/ 加进 .gitignore 即可（还没处理）。
- **学到的道理**：程序自动生成的数据文件，一律别进 git，不然每次运行都是噪音。
- [ ] 我学会了

### 4. 超长段落会被整段硬塞进一个块 ⚠️
- **发生了啥**：如果笔记里有一段特别长（超过 400 字），切块程序不会把它切开，而是整段塞进去，导致这个"块"又大又笨。
- **为啥会这样**：切块逻辑对"超长段落"没有兜底分支。
- **怎么解决**：未来在段落内部再按句子二次切分（还没做）。
- **学到的道理**：写处理逻辑时要想到"输入不规整"的情况，留个兜底。
- [ ] 我学会了

---

## 📚 二、AI 问答相关的坑（Agent 与 LLM 层）

### 5. IDE 手滑，多打了一行没用的代码 ✅
- **发生了啥**：代码编辑器自动补全时手滑，给问答程序加了一行没人用的导入。程序居然能跑，因为那个库碰巧被别的依赖装上了。
- **为啥会这样**：IDE 自动补全的"事故"，加上没人检查改动的文件里多出来的行。
- **怎么解决的**：删掉那行没用的导入（代码评审时发现的）。
- **学到的道理**：提交前看一眼自己改了什么；"能跑"不等于"代码是对的"。
- [ ] 我学会了

### 6. 明明找到了答案，却误判成"没找到" ✅
- **发生了啥**：一个问题拆成好几个词去搜，搜完合并到一起，但没按分数排序就拿了第一个判断——第一个恰好分低，系统就以为"没找到"，白白去网上搜。
- **为啥会这样**：合并多路搜索结果后忘了排序，却直接看"第一个"的分数。
- **怎么解决的**：合并后先按相似度从高到低排序；返回答案前再用最高分判断一次（双保险）。
- **学到的道理**：多路结果合并后必须排序，判断要基于"最好的那个"，不是"第一个"。
- [ ] 我学会了

### 7. 检索没命中时，把工具算好的结果弄丢了 ✅
- **发生了啥**：你问"2 的 10 次方"，程序先查知识库——没查到，就自动去调计算工具。结果工具算出了 1024，但程序在"没资料"分支里提前返回，把 1024 丢了，最后只能说"凭常识大概是 1024"。
- **为啥会这样**：判断"有没有料"时只看了检索结果，没把工具结果算进去；而工具结果恰恰在检索落空时最有用。
- **怎么解决的**：改成：检索结果或工具结果，只要有一样就不算"没资料"。
- **学到的道理**：判断有没有材料，要检查所有来源，不能只盯一条路。
- [ ] 我学会了

### 8. 审查员看不见工具结果，把对的答案打回去重写 ✅
- **发生了啥**：多 Agent 流程里有个"审查员"负责检查答案有没有依据。但审查员只看了检索到的资料，看不到工具算出的结果——于是工具型答案被判"没依据"，打回重写。
- **为啥会这样**：审查员的"参考材料"和回答者的"输入材料"不一致。
- **怎么解决的**：审查时把工具结果也拼进参考材料里。
- **学到的道理**：审查的人/程序，看到的材料必须比干活的人更全，否则会冤枉人。
- [ ] 我学会了

### 9. 每次提问都多问一次 AI"要不要用工具" ⚠️
- **发生了啥**：就算知识库里已经找到了答案，程序还是多一步：问 AI 要不要调用工具。多花一次调用、多等几秒。
- **为啥会这样**：流程图里这条线是固定的，不管有没有资料都走一遍。
- **怎么解决**：未来改成：有答案就直接答，只有没找到/需要计算时才问工具（还没做）。
- **学到的道理**：流程里"每步都跑"和"按需跑"差别很大，想清楚哪些步骤可以跳过。
- [ ] 我学会了

### 10. 给子进程的代码模板用错了拼接方式 ✅
- **发生了啥**：程序要在一个"小房间"（子进程）里执行用户代码。模板代码里有一句"出错时显示错误类型"——但用 f-string 拼接时，这句在"外面"就被提前执行了，结果子进程里反而拿不到错误信息。
- **为啥会这样**：模板里"运行时才该计算"的部分，被父进程提前展开求值了。
- **怎么解决的**：模板改用普通字符串拼接，把错误类型的计算留到子进程里执行。
- **学到的道理**：跨进程传代码时，"该在那边算的"绝对不要提前算，要分清拼接时机。
- [ ] 我学会了

### 11. 用"线程"跑代码，超时杀不死 ✅
- **发生了啥**：执行用户代码用线程实现，遇到死循环或失控代码，超时后杀不掉，还可能把整个程序拖垮。
- **为啥会这样**：线程超时只是"提醒"，控制不了已经在跑的失控代码。
- **怎么解决的**：改成用"子进程"跑（subprocess + 超时），超时直接终止进程，互不影响；还处理了语法错误、崩溃退出码等情况。
- **学到的道理**：执行不可信的代码要按"进程"隔离，不是线程——就像把危险品放独立仓库而不是自家客厅。
- [ ] 我学会了

### 12. AI 返回空内容时没防护 ✅
- **发生了啥**：调 AI 接口偶尔会返回空结果，程序直接拿空内容用，会出问题。
- **为啥会这样**：没考虑外部服务"什么都没返回"的情况。
- **怎么解决的**：空响应统一按"没有结果"处理，mock 模式（没配 key 时）也走安全分支。
- **学到的道理**：外部服务的返回永远要防空，调用方要有默认兜底。
- [ ] 我学会了

---

## 📚 三、聊天记录相关的坑（会话与记忆）

### 13. 会话文件名多了个重复前缀 ✅
- **发生了啥**：每个聊天会话有个编号（s_abc123），存文件时又自动加了一遍 s_，结果文件名变成 s_s_abc123.json。程序内部自己调用没事，但任何"按编号找文件"的外部代码都会找不到。
- **为啥会这样**：生成编号和拼文件名各加了一次前缀，两边约定不一致。
- **怎么解决的**：拼文件名时去掉重复的前缀，编号风格保持不变。
- **学到的道理**："碰巧自己能跑"的代码是隐藏地雷，换个人或换个调用方式就炸。
- [ ] 我学会了

### 14. 改存储格式没做兼容，老聊天记录差点丢 ✅
- **发生了啥**：修完上面的 bug 后，以前存的老格式文件会"找不到"，聊天记录可能静默丢失。
- **为啥会这样**：改了文件命名规则，但没处理老文件。
- **怎么解决的**：加了一段一次性迁移代码：启动时自动把老文件按新规则改名（幂等、只跑一次）。
- **学到的道理**：改存储格式必须带迁移，宁可多写代码也别丢用户数据。
- [ ] 我学会了

### 15. 长期记忆模块写好了，但没接进问答 → 已接线 ✅
- **发生了啥**：代码里有个"长期记忆"模块（记住用户偏好、目标），读写功能都写完了，但一开始没有任何地方调用它——问答流程完全没用到，记忆文件也从来没生成过。
- **为啥会这样**：写了数据层就以为完成了，忘了"接线"进主流程。
- **怎么解决的**：已把记忆接进问答：提问前把记忆内容拼进 system prompt，回答后后台线程异步提炼新事实存进去（17 个测试用例覆盖）。
- **学到的道理**：数据层写完 ≠ 功能完成。模块没接进主流程就是死代码，等于没做。
- [ ] 我学会了

---

## 📚 四、网页相关的坑（Web 与前端）

### 16. Windows 控制台打印 emoji 会崩 🟡
- **发生了啥**：在 Windows 的黑色命令行里打印 📁 这类表情符号，会直接报编码错误，严重时整个网页接口报 500。
- **为啥会这样**：Windows 控制台默认用 GBK 编码，不认 emoji。
- **怎么解决的**：约定：代码里打印日志一律用纯文字标记（如 [上传]），不碰 emoji。
- **学到的道理**：跨平台写代码，日志输出用最朴素的字符最稳。
- [ ] 我学会了

### 17. 前端脚本有一个语法错误，整个网页全失灵 ✅
- **发生了啥**：网页的前端 JS 里有一个小语法错误，导致整个页面报错：上传不能用、提问不能用、什么按钮都没反应。
- **为啥会这样**：改了前端代码但没做语法检查，也没在浏览器里验证。
- **怎么解决的**：修复语法错误，并加了 node --check 语法校验。
- **学到的道理**：前端改完必须验证，"看着对"不等于"浏览器不报错"。
- [ ] 我学会了

### 18. 向量库的遥测噪音 🟡
- **发生了啥**：用向量库组件时，控制台会打出一些遥测相关的报错噪音，不影响功能但很烦。
- **为啥会这样**：第三方库默认开了匿名遥测，环境里打印报错。
- **怎么解决的**：设置环境变量 ANONYMIZED_TELEMETRY=False 关掉。
- **学到的道理**：第三方库的环境级小坑记一笔，省得每次困惑。
- [ ] 我学会了

### 19. AI 回答里的表格显示成原始竖线文本 ✅
- **发生了啥**：AI 回答里用 `| 列1 | 列2 |` 写的表格，在前端直接显示成原始的竖线文本，没有渲染成真正的表格。
- **为啥会这样**：前端的轻量 Markdown 渲染器只做了标题、加粗、列表、代码块，**漏了表格语法**。
- **怎么解决的**：给渲染器加了表格正则解析——连续竖线分隔行 → 表头/分隔行/数据行 → 输出 `<table>`，加了表头蓝底、斑马纹 CSS。
- **学到的道理**：自己写 Markdown 渲染器要列全语法清单，不能只做"常用的几个"；表格是 AI 回答里的高频格式。
- [ ] 我学会了

### 20. 流式打字机输出：同步图引擎和流式 LLM 怎么桥接？ ✅
- **发生了啥**：要做前端打字机效果，但后端 LangGraph 是同步执行的（规划→检索→工具→回答→审查），回答节点内部调 LLM 生成文本——怎么把 LLM 的每个字实时推到前端？
- **为啥会这样**：`graph.stream()` 只能拿到节点级状态（节点开始/结束），拿不到回答节点内部 LLM 的增量 token。如果等回答节点结束才推文本，前端就是"等 10 秒然后一次性蹦出全文"。
- **怎么解决的**：用 `threading.local`（线程局部存储）挂载当前线程的回调函数；回答节点流式调 LLM 时，每拿到一个字就同时拼进返回文本 + 调回调推队列；主线程循环消费队列实时 yield SSE 事件。节点级状态和 token 级增量是两个粒度，分别处理。
- **学到的道理**：同步流程和流式输出的桥接要靠"线程局部变量 + 回调 + 队列"；多请求并发时 threading.local 保证回调不串台。
- [ ] 我学会了

---

## 📚 五、依赖与工程相关的坑

### 21. 两个库版本打架，AI 客户端崩了 ✅
- **发生了啥**：装依赖时没锁版本，结果 httpx 更新到新版后，openai 库调用就崩。
- **为啥会这样**：httpx 0.28 删了一个旧参数，openai 旧版本不兼容。
- **怎么解决的**：在 requirements.txt 里锁住 httpx<0.28，并注释写明原因。
- **学到的道理**：锁版本一定要写原因注释，不然后人看到会以为是多余的、删掉又崩。
- [ ] 我学会了

### 22. 上传的文件名没检查，可能被"路径穿越"攻击 ✅
- **发生了啥**：用户上传文件的文件名如果直接拿去拼路径，构造 ../ 这种名字就可能写到知识库外面去。
- **为啥会这样**：用户输入没校验就拼路径，本地工具也要防。
- **怎么解决的**：加白名单校验：文件名不允许带 /、\、.. 等任何路径成分，主题名也只允许单层目录。
- **学到的道理**：任何"用户输入 → 拼路径"的地方都要净化，本地项目也不例外。
- [ ] 我学会了

### 23. 待审查文件放错位置会被误入库 🟡
- **发生了啥**：上传的文件先进"待审查区"，等人工确认。如果待审查区放在笔记目录里，程序扫描时会把没确认的文件也入库。
- **为啥会这样**：扫描器会递归扫整个笔记目录。
- **怎么解决的**：把待审查区放在笔记目录外面，物理隔离，从根上避免误扫。
- **学到的道理**：扫描类程序，"待处理区"要和"正式区"隔开，不能靠口头约定。
- [ ] 我学会了

### 24. Python 文件是 CRLF 行尾，编辑工具匹配失败 🟡
- **发生了啥**：用编辑工具修改 main.py 和 kb/*.py 时，明明内容看起来一样，却报"找不到"。
- **为啥会这样**：这些 Python 文件是 CRLF（\r\n）行尾，编辑工具按 LF（\n）匹配，\r 导致精确匹配失败。前端 HTML 是 LF 就正常。
- **怎么解决的**：修改 CRLF 文件时必须用 Python 脚本 `io.open(encoding="utf-8", newline="")` 读写，精确控制行尾；不能用普通编辑工具。
- **学到的道理**：跨平台项目要统一行尾，或至少记清楚哪些文件是 CRLF；工具链对行尾敏感时选错工具浪费大量时间。
- [ ] 我学会了

---

## 📚 六、标签自治体系相关的坑（新项目亮点）

### 25. 直觉建议的 0.85 标签阈值，实测 0% 命中率 ✅
- **发生了啥**：做标签自动匹配时，直觉觉得"相似度超过 0.85 才复用标签"比较稳妥。但实测 101 篇文档的 Top1 标签相似度：最低 0.467、中位数 0.597、最高才 0.741——0.85 阈值一篇都命中不了，方案完全失效。
- **为啥会这样**：bge 模型在"短标签（几个字）vs 长文档（几百字）"场景下，分数天然偏低。0.85 是"句子对句子"的阈值，不适合"标签对文档"。
- **怎么解决的**：用真实数据校准后改为**三分层**：≥0.60 直接复用（免 LLM，命中率 46.5%）、0.45~0.60 让 LLM 确认候选、<0.45 让 LLM 生成新标签并写回标签库。
- **学到的道理**：阈值绝对不能拍脑袋，必须用真实数据分布校准；用户的直觉阈值在不同语义场景下可能完全失效。
- [ ] 我学会了

### 26. 标签模块和打标模块互相 import，死循环 ✅
- **发生了啥**：标签库模块（tag_store）需要调用打标模块（tagger）的"清洗标签"函数；打标模块又需要调用标签库的"检索/添加标签"函数——两个模块在顶层互相 import，Python 导入时死循环卡住。
- **为啥会这样**：模块级循环依赖，A import B 的同时 B import A。
- **怎么解决的**：标签库模块不在顶层 import 打标模块，只在函数内部**延迟导入**（用到时才 import），循环被打破。
- **学到的道理**：循环依赖的标准解法是"延迟导入 + 单向依赖"——被依赖方不在顶层 import 依赖方，只在函数内部按需 import。
- [ ] 我学会了

### 27. 标签跨领域误匹配（ES6 文档被打上"Python基础"标签）⚠️
- **发生了啥**：0.60 复用线在"通用编程概念"上容易跨领域撞车——比如 ES6（JavaScript）文档的 Top1 标签匹配到"Python基础"（相似度 0.60），触发复用，但 ES6 和 Python 是完全不同的领域。
- **为啥会这样**："变量""函数""循环"等基础概念在 Python 和 JS 里都有，向量距离近，0.60 线不够高。
- **怎么解决**：观察项——如果误匹配率高，把复用线从 0.60 上调到 0.65（更多文档走 LLM 确认路径，减少误复用）。
- **学到的道理**：阈值校准要看"误报率"，不能只看"命中率"；0.60 命中率 46.5% 看起来不错，但如果其中 10% 是跨领域误匹配，实际有效率就只有 42%。
- [ ] 我学会了

---

## 📚 七、环境与部署相关的坑

### 28. torch import 卡死超过 3 分钟，服务起不来 ⚠️
- **发生了啥**：重启服务时，`import torch`（sentence_transformers 依赖）卡死超过 3 分钟无响应，CPU 模式和 CUDA 模式都卡住，导致整个 Web 服务起不来（浏览器显示错误页）。
- **为啥会这样**：深度学习依赖（torch/sentence_transformers）是启动慢的重灾区——第一次 import 需要加载大量 DLL、初始化，可能需要几分钟。可能原因：杀毒软件扫描 torch DLL、torch 安装损坏、磁盘 IO 慢、缓存损坏。之前服务能正常启动，说明是环境临时异常。
- **怎么解决**：临时绕过方案——修改 embedding.py 强制走 fallback 哈希向量器（不加载 sentence_transformers），服务能秒起，但检索质量下降。根本解决需要检查杀毒软件、重装 torch、或等环境恢复。
- **学到的道理**：深度学习依赖是启动慢的重灾区，第一次 import 可能需要几分钟；生产环境要做"启动健康检查 + 超时告警"，别等用户报"页面打不开"才发现。个人项目也要记一笔：torch import 慢 ≠ 代码坏了。
- [ ] 我学会了

---

## 🎯 已掌握汇总（自己数一下勾了哪些）

| # | 坑 | 状态 | 学会？ |
|---|---|---|---|
| 1 | 笔记切块太大，超过向量模型上限 | ✅ | [ ] |
| 2 | 同名文件互相打架 | ✅ | [ ] |
| 3 | 台账文件被 git 盯上 | ⚠️ | [ ] |
| 4 | 超长段落整段硬塞 | ⚠️ | [ ] |
| 5 | IDE 手滑多打一行 | ✅ | [ ] |
| 6 | 合并结果没排序，误判没找到 | ✅ | [ ] |
| 7 | 检索落空把工具结果丢了 | ✅ | [ ] |
| 8 | 审查员看不到工具结果 | ✅ | [ ] |
| 9 | 每次提问都多问一次 AI | ⚠️ | [ ] |
| 10 | 子进程模板用错拼接方式 | ✅ | [ ] |
| 11 | 线程超时杀不死 | ✅ | [ ] |
| 12 | AI 返回空内容没防护 | ✅ | [ ] |
| 13 | 会话文件名重复前缀 | ✅ | [ ] |
| 14 | 改格式没做兼容迁移 | ✅ | [ ] |
| 15 | 长期记忆没接线 → 已接线 | ✅ | [ ] |
| 16 | Windows 控制台 emoji 崩溃 | 🟡 | [ ] |
| 17 | 前端语法错整页失灵 | ✅ | [ ] |
| 18 | 向量库遥测噪音 | 🟡 | [ ] |
| 19 | 前端表格渲染缺失 | ✅ | [ ] |
| 20 | 流式输出多线程回调桥接 | ✅ | [ ] |
| 21 | 依赖版本打架 | ✅ | [ ] |
| 22 | 文件名路径穿越 | ✅ | [ ] |
| 23 | 待审查文件误入库 | 🟡 | [ ] |
| 24 | CRLF 行尾编辑工具失败 | 🟡 | [ ] |
| 25 | 标签阈值 0.85 实测 0% 命中 | ✅ | [ ] |
| 26 | 标签模块循环依赖 | ✅ | [ ] |
| 27 | 标签跨领域误匹配 | ⚠️ | [ ] |
| 28 | torch import 卡死 | ⚠️ | [ ] |

---

*想要"勾选自动移到最后 + 删除按钮 + 进度条"，打开同目录下的「开发记录-新手版.html」。*
